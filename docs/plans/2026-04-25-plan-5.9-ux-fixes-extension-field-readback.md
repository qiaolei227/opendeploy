# Plan 5.9(非正式)— UX 修补 + 扩展字段反查 + 字段布局放置

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修掉用户在 2026-04-25 演示 demo 中暴露的 5 个问题——流式/工具运行无可见反馈、markdown 表格列宽不对齐、agent 张口"按 F5"(BOS 刷新方式不固定)、扩展字段写入无反查工具(只能用查原厂字段的工具骗自己)、新加字段默认 (Top=10, Left=10, ZOrder=99) 落在左上角和现有字段重叠。

**Architecture:**
- UI 部分(任务 1-2):纯渲染层修改,沿用已有 Zustand `ChatMessage` / `MessageBlock` 数据,不动 streaming pipeline。
- Prompt 部分(任务 3):4 个文件里把硬编码的 "F5" 改成"刷新"通称。
- 工具部分(任务 4-6):新增 `kingdee_get_extension_fields`,先在 `bos-xml.ts` 加纯解析函数(无 DB)→ `bos-writer.ts` 加 `listExtensionFields`(读 FKERNELXML 调 parser)→ `bos-write-tools.ts` 加工具定义 + 在 erp-rules prompt 里把"反查"硬规则补上 add_field 闭环。
- 字段布局(任务 7):v0.1 务实方案——不去 parse 父对象 1MB+ 的 FKERNELXML 推算下一个空位,而是把新字段默认 `Top=9000 / ZOrder=9999`,落到容器底部(目视上不和现有字段重叠),给用户"在 BOS Designer 拖一下"的明确提示。后续 cycle 再做"读父布局自动放置"。

**Tech Stack:** TypeScript 6 strict / React 19 + Zustand / Vitest 4 / 现有 mssql + bos-* 模块栈。无新增依赖。

---

## 文件清单

| 用途 | 创建/修改 | 路径 |
|---|---|---|
| Thinking 指示器派生函数 | Modify | `src/renderer/components/Message.tsx` |
| Thinking 指示器派生函数测试 | Create | `tests/renderer/message-pending.test.ts` |
| 新 i18n key `messages.toolRunning` | Modify | `src/renderer/i18n/locales/zh-CN/common.json` / `en-US/common.json` |
| Markdown 表格 CSS | Modify | `src/renderer/styles/design-system.css` |
| 系统提示词 F5 → 刷新 | Modify | `src/main/agent/prompts/erp-rules/k3cloud.md` / `src/main/agent/bos-write-tools.ts`(reminder 字符串) |
| `parseFieldsFromKernelXml` 解析器 | Modify | `src/main/erp/k3cloud/bos-xml.ts` |
| `parseFieldsFromKernelXml` 测试 | Modify | `tests/erp/bos-xml.test.ts` |
| `listExtensionFields` DB 函数 | Modify | `src/main/erp/k3cloud/bos-writer.ts` |
| `listExtensionFields` 测试 | Modify | `tests/erp/bos-writer.test.ts` |
| `kingdee_get_extension_fields` 工具 | Modify | `src/main/agent/bos-write-tools.ts` |
| 工具集成测试 | Modify | `tests/agent/k3cloud-tools.test.ts` |
| `addFieldToExtension` 默认位次改进 | Modify | `src/main/erp/k3cloud/bos-xml.ts`(`defaultNumericGenerator`) + `bos-writer.ts` |
| 默认位次测试 | Modify | `tests/erp/bos-xml.test.ts` |

---

## Task 1: 流式 thinking / 工具执行 视觉反馈

**问题**:`Message.tsx` 现在只在 `!hasAnyContent(message)` 时显示"思考中…"。一旦 LLM 吐出第一段文字或一个 tool_call,thinking 就消失。后续 LLM 在思考下一步、或工具正在执行时,turn body 末尾没任何"还在动"的视觉信号,用户怀疑卡死。Tool card 内部虽然有 "执行中" 状态徽章,但视觉上太轻。

**Files:**
- Modify: `src/renderer/components/Message.tsx`(整个文件,加派生函数 + 调整渲染条件)
- Create: `tests/renderer/message-pending.test.ts`
- Modify: `src/renderer/i18n/locales/zh-CN/common.json`(line ~212 附近,`messages` 节点下加 key)
- Modify: `src/renderer/i18n/locales/en-US/common.json`(同步加)

- [ ] **Step 1: 写失败测试 (派生函数 derivePendingActivity)**

新建 `tests/renderer/message-pending.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { derivePendingActivity } from '../../src/renderer/components/Message';
import type { ChatMessage } from '../../src/renderer/stores/chat-store';

function msg(overrides: Partial<ChatMessage>): ChatMessage {
  return {
    id: 'm1',
    role: 'assistant',
    content: '',
    createdAt: '2026-04-25T00:00:00Z',
    isStreaming: true,
    ...overrides
  };
}

describe('derivePendingActivity', () => {
  it('returns null when message is not streaming', () => {
    expect(derivePendingActivity(msg({ isStreaming: false }))).toBeNull();
  });

  it('returns thinking when streaming with no blocks yet', () => {
    expect(derivePendingActivity(msg({ blocks: [] }))).toEqual({ kind: 'thinking' });
  });

  it('returns awaiting-tool with name when last block is a running tool', () => {
    const result = derivePendingActivity(msg({
      blocks: [{ type: 'tool_use', callId: 'c1' }],
      toolCalls: [{ id: 'c1', name: 'kingdee_list_extensions', args: '{}' }]
    }));
    expect(result).toEqual({ kind: 'awaiting-tool', toolName: 'kingdee_list_extensions' });
  });

  it('returns thinking when last block is a finished tool (has result)', () => {
    expect(derivePendingActivity(msg({
      blocks: [{ type: 'tool_use', callId: 'c1' }],
      toolCalls: [{ id: 'c1', name: 'kingdee_list_extensions', args: '{}', result: '{"count":0}' }]
    }))).toEqual({ kind: 'thinking' });
  });

  it('returns null when last block is text mid-stream (cursor handles it)', () => {
    expect(derivePendingActivity(msg({
      blocks: [{ type: 'text', text: '我先看一下扩展' }]
    }))).toBeNull();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
pnpm vitest run tests/renderer/message-pending.test.ts
```
Expected: FAIL `derivePendingActivity is not a function`(模块没导出)

- [ ] **Step 3: 实现 derivePendingActivity 并改 Message.tsx 渲染**

在 `src/renderer/components/Message.tsx` 文件顶部 import 区下面、`ToolCallCard` 之前,加导出函数:

```ts
export type PendingActivity =
  | { kind: 'thinking' }
  | { kind: 'awaiting-tool'; toolName: string };

/**
 * What "still working" indicator (if any) to show at the bottom of an
 * assistant turn. Returns null when the cursor on a streaming text block
 * is enough — extra labels under live text just add noise.
 */
export function derivePendingActivity(message: ChatMessage): PendingActivity | null {
  if (!message.isStreaming) return null;
  const blocks = message.blocks ?? [];
  if (blocks.length === 0) return { kind: 'thinking' };
  const last = blocks[blocks.length - 1];
  if (last.type === 'text') return null; // cursor handles it
  // last block is a tool_use — running vs finished decides label
  const call = (message.toolCalls ?? []).find((c) => c.id === last.callId);
  if (call && !call.result) {
    return { kind: 'awaiting-tool', toolName: call.name };
  }
  return { kind: 'thinking' };
}
```

替换 `Message` 组件末尾(line 130-135)的旧条件:

```tsx
// 旧:
{message.isStreaming && !hasAnyContent(message) && (
  <div className="turn-thinking">{t('messages.thinking')}</div>
)}

// 新:
{(() => {
  const pending = derivePendingActivity(message);
  if (!pending) return null;
  const label =
    pending.kind === 'thinking'
      ? t('messages.thinking')
      : t('messages.toolRunning', { name: pending.toolName });
  return <div className="turn-thinking">{label}</div>;
})()}
```

删掉 `hasAnyContent` 函数(line 56-61),不再需要。

- [ ] **Step 4: 加 i18n key**

`src/renderer/i18n/locales/zh-CN/common.json`,在 `messages.thinking` 那行下面同一对象内加:

```json
    "thinking": "思考中…",
    "toolRunning": "执行 {{name}} 中…",
```

`src/renderer/i18n/locales/en-US/common.json`,对应位置加:

```json
    "thinking": "thinking…",
    "toolRunning": "running {{name}}…",
```

- [ ] **Step 5: 跑测试 + typecheck**

```bash
pnpm vitest run tests/renderer/message-pending.test.ts
pnpm typecheck
```
Expected: 5 passed; typecheck clean.

- [ ] **Step 6: 跑 i18n parity 测试**

```bash
pnpm vitest run tests/i18n.test.ts
```
Expected: PASS(zh-CN / en-US 两边都加了 toolRunning,parity 通过)。

- [ ] **Step 7: 提交**

```bash
git add src/renderer/components/Message.tsx tests/renderer/message-pending.test.ts \
  src/renderer/i18n/locales/zh-CN/common.json src/renderer/i18n/locales/en-US/common.json
git commit -m "feat(ui): 流式与工具执行中均显示活动指示器"
```

---

## Task 2: Markdown 表格列宽对齐

**问题**:`design-system.css` 没有 `.md table` 样式,react-markdown 渲染出的 `<table>` 没边框、列宽乱跳(2026-04-25 截图实证)。

**Files:**
- Modify: `src/renderer/styles/design-system.css`(在 `.turn-body` 块之后插入)

- [ ] **Step 1: 加表格样式**

在 `src/renderer/styles/design-system.css` 第 474 行(`.turn-body strong { font-weight: 600; }`)之后插入:

```css
/* Markdown table — 顾问演示场景常用 "项 / 内容" 两列对照表 */
.md table {
  width: 100%;
  border-collapse: collapse;
  margin: 8px 0;
  font-size: 13px;
  table-layout: auto;
}
.md thead th {
  text-align: left;
  font-weight: 600;
  background: var(--surface-2);
  border-bottom: 1px solid var(--border);
  padding: 6px 10px;
}
.md tbody td {
  padding: 6px 10px;
  border-bottom: 1px solid var(--border);
  vertical-align: top;
}
.md tbody tr:last-child td { border-bottom: none; }
.md table code {
  /* 表格里行内 code 不要继承 .turn-body 的 13px,缩小一档免得撑列 */
  font-size: 11.5px;
}
```

- [ ] **Step 2: 启动 dev 验证**

```bash
pnpm dev
```

在对话里输入 "用 markdown 表格列出 5 个金蝶模块、各 3 个特性",肉眼确认:
- 列宽按内容自适应,表头加粗有底色
- 行间有横线分隔
- 表格内 inline code 不撑破列宽

(`pnpm dev` 跑起来后用浏览器/Electron 看,验证完 Ctrl+C)

- [ ] **Step 3: 提交**

```bash
git add src/renderer/styles/design-system.css
git commit -m "fix(ui): 渲染 markdown 表格添加列对齐与边框样式"
```

---

## Task 3: 系统提示词 / 工具 reminder 中的 F5 → 通用 "刷新"

**问题**:agent 在 demo 里说"BOS Designer 按 F5 刷新",但 BOS Designer 不是 F5 刷新(用户实证)。改成不指定按键的通用表述,把"具体怎么刷新"留给用户自己知道。

**Files:**
- Modify: `src/main/agent/prompts/erp-rules/k3cloud.md`(line 60)
- Modify: `src/main/agent/bos-write-tools.ts`(line 184, 225, 351 的 reminder 字符串)

- [ ] **Step 1: 改 prompt**

`src/main/agent/prompts/erp-rules/k3cloud.md` 第 60 行:

```diff
-   - **BOS Designer 按 F5 刷新**才能看到新扩展;**已打开的客户端表单可能需要重登**
+   - **在 BOS Designer 中刷新扩展**(工具栏刷新按钮 / 关闭重开扩展)才能看到新扩展;**已打开的客户端表单可能需要重登**
```

- [ ] **Step 2: 改 tool reminder 字符串**

`src/main/agent/bos-write-tools.ts`,3 处 reminder 字符串里的 "F5" 全部去掉:

第 184 行:
```diff
-          reminder:
-            '请在 BOS Designer 按 F5 刷新扩展列表;新建销售订单时客户端可能需重登一次才能加载新插件。如需共享给团队,去 BOS Designer 点一次"同步"(SVN)。'
+          reminder:
+            '请在 BOS Designer 中刷新扩展列表(工具栏刷新按钮);新建销售订单时客户端可能需重登一次才能加载新插件。如需共享给团队,去 BOS Designer 点一次"同步"(SVN)。'
```

第 225 行:
```diff
-          reminder:
-            '请在 BOS Designer 按 F5 刷新,客户端可能需重登一次。团队协作用 SVN 的话去 BOS 点一次"同步"。'
+          reminder:
+            '请在 BOS Designer 中刷新扩展,客户端可能需重登一次。团队协作用 SVN 的话去 BOS 点一次"同步"。'
```

第 351 行:
```diff
-          reminder:
-            '字段已写入 DB。去 BOS Designer F5 刷新扩展就能看到新字段 —— 位次 / 坐标 Designer 会自动 normalize。如用 SVN 同步共享给团队, 记得点一次"同步"。'
+          reminder:
+            '字段已写入 DB。去 BOS Designer 中刷新扩展(工具栏刷新按钮)就能看到新字段 —— 位次 / 坐标 Designer 会自动 normalize。如用 SVN 同步共享给团队, 记得点一次"同步"。'
```

- [ ] **Step 3: 跑现有测试确认未破坏**

```bash
pnpm vitest run tests/agent/k3cloud-tools.test.ts
```
Expected: PASS(原测试不依赖 reminder 字面)。

- [ ] **Step 4: 提交**

```bash
git add src/main/agent/prompts/erp-rules/k3cloud.md src/main/agent/bos-write-tools.ts
git commit -m "fix(prompt): BOS 刷新方式不写死 F5 改用通用表述"
```

---

## Task 4: bos-xml.ts 加 parseFieldsFromKernelXml 纯解析器

**问题**:扩展字段写完后没法反查——`kingdee_get_fields` 查的是原厂字段,扩展字段在 `T_META_OBJECTTYPE.FKERNELXML` 里。先在 bos-xml 层加纯函数 `parseFieldsFromKernelXml(xml): ExtensionFieldMeta[]`,无 DB,可单测。

`<TextField>` 节点是 Elements 下 Form 的兄弟节点(见 `bos-xml.ts:340`):

```xml
<TextField ElementType="1" ElementStyle="0">
  <ConditionType>0</ConditionType>
  <PropertyName>F_DEMO</PropertyName>
  <FieldName>F_DEMO</FieldName>
  <ListTabIndex>9999</ListTabIndex>
  <Name>演示字段</Name>
  <Id>...</Id>
  <Key>F_DEMO</Key>
</TextField>
```

Caption 在 LayoutInfos 里:

```xml
<TextFieldAppearance ...>
  <Key>F_DEMO</Key>
  <Container>FTAB_P0</Container>
  ...
  <Caption>演示字段</Caption>
  ...
</TextFieldAppearance>
```

所以 parser 要遍历 TextField 收 base 信息,再按 Key 关联 TextFieldAppearance 拿 caption / container。

**Files:**
- Modify: `src/main/erp/k3cloud/bos-xml.ts`(在文件末尾的 `insertTextFieldIntoKernelXml` 函数之前/之后加新导出)
- Modify: `tests/erp/bos-xml.test.ts`

- [ ] **Step 1: 写失败测试**

在 `tests/erp/bos-xml.test.ts` 文件末尾追加 describe block:

```ts
import {
  insertTextFieldIntoKernelXml,
  parseFieldsFromKernelXml,
  type ExtensionFieldMeta
} from '../../src/main/erp/k3cloud/bos-xml';

describe('parseFieldsFromKernelXml', () => {
  it('returns [] for XML without TextField', () => {
    const xml = buildExtensionKernelXml(EXT, []);
    expect(parseFieldsFromKernelXml(xml)).toEqual([]);
  });

  it('parses one inserted text field with caption from appearance', () => {
    const base = buildExtensionKernelXml(EXT, []);
    const xml = insertTextFieldIntoKernelXml(base, {
      spec: { key: 'F_DEMO', caption: '演示字段' }
    });
    const fields = parseFieldsFromKernelXml(xml);
    expect(fields).toEqual<ExtensionFieldMeta[]>([
      {
        key: 'F_DEMO',
        type: 'text',
        caption: '演示字段',
        propertyName: 'F_DEMO',
        fieldName: 'F_DEMO',
        container: 'FTAB_P0'
      }
    ]);
  });

  it('parses multiple fields keeping insertion order', () => {
    let xml = buildExtensionKernelXml(EXT, []);
    xml = insertTextFieldIntoKernelXml(xml, { spec: { key: 'F_A', caption: '甲' } });
    xml = insertTextFieldIntoKernelXml(xml, { spec: { key: 'F_B', caption: '乙' } });
    const fields = parseFieldsFromKernelXml(xml);
    expect(fields.map((f) => f.key)).toEqual(['F_A', 'F_B']);
    expect(fields.find((f) => f.key === 'F_A')?.caption).toBe('甲');
    expect(fields.find((f) => f.key === 'F_B')?.caption).toBe('乙');
  });

  it('falls back to Name when no matching appearance Caption', () => {
    // 手工构造一个只有 TextField 没有 Appearance 的 XML — 实际场景罕见
    // (Designer 总会写 Appearance), 但 parser 不能因此 crash.
    const xml =
      '<FormMetadata><BusinessInfo><BusinessInfo><Elements>' +
      '<Form action="edit" oid="BOS_BillModel" ElementType="100" ElementStyle="0">' +
      `<Id>${EXT}</Id><FormPlugins/>` +
      '</Form>' +
      '<TextField ElementType="1" ElementStyle="0">' +
      '<PropertyName>F_X</PropertyName><FieldName>F_X</FieldName>' +
      '<Name>仅名称</Name><Id>x</Id><Key>F_X</Key>' +
      '</TextField>' +
      '</Elements></BusinessInfo></BusinessInfo></FormMetadata>';
    const fields = parseFieldsFromKernelXml(xml);
    expect(fields).toEqual<ExtensionFieldMeta[]>([
      {
        key: 'F_X',
        type: 'text',
        caption: '仅名称',
        propertyName: 'F_X',
        fieldName: 'F_X',
        container: undefined
      }
    ]);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
pnpm vitest run tests/erp/bos-xml.test.ts -t parseFieldsFromKernelXml
```
Expected: FAIL `parseFieldsFromKernelXml is not exported`。

- [ ] **Step 3: 实现 parser**

在 `src/main/erp/k3cloud/bos-xml.ts` 文件末尾追加:

```ts
// ─── Field reading ────────────────────────────────────────────────────

export interface ExtensionFieldMeta {
  /** 表单 Key, 如 'F_DEMO' (BOS Designer 中的"字段标识")。*/
  key: string;
  /** v0.1 只解析 TextField → 'text';后续支持其他类型时此处分支。*/
  type: 'text';
  /** 显示标签;优先取 Appearance 的 Caption,次取 TextField 的 Name。*/
  caption: string;
  propertyName: string;
  fieldName: string;
  /** 布局容器 Key (如 'FTAB_P0'), Appearance 缺失时 undefined。*/
  container: string | undefined;
}

/**
 * 解析扩展 FKERNELXML 里的扩展字段定义(目前只识别 <TextField>)。
 * 流程:① 遍历 Elements 直接子级的 <TextField> 收 base info;
 * ② 在 LayoutInfos/Appearances/TextFieldAppearance 里按 Key 配对捞 Caption / Container。
 */
export function parseFieldsFromKernelXml(xml: string): ExtensionFieldMeta[] {
  if (!xml) return [];

  // Step 1: 取 Appearance 的 Key → {caption, container} 映射
  const appearanceByKey = new Map<string, { caption?: string; container?: string }>();
  collectAppearances(xml, appearanceByKey);

  // Step 2: 走每一个直接子级 <TextField>
  const fields: ExtensionFieldMeta[] = [];
  type Frame = { tag: string; bodyStart: number; isTextField: boolean };
  const stack: Frame[] = [];

  for (const tk of iterateTagTokens(xml)) {
    if (tk.isSelfClose) continue;
    if (!tk.isClose) {
      stack.push({ tag: tk.tag, bodyStart: tk.end, isTextField: tk.tag === 'TextField' });
      continue;
    }
    const frame = stack.pop();
    if (!frame || !frame.isTextField) continue;
    // 嵌在 LayoutInfos 下的 <TextField> 不算字段定义本身 — 只看 stack 底部
    // 是 Elements 直系的(stack 此刻应只剩 FormMetadata/BusinessInfo×2/Elements)。
    if (stack.some((f) => f.tag === 'LayoutInfos' || f.tag === 'Appearances')) continue;

    const nodeBody = xml.substring(frame.bodyStart, tk.start);
    const key = findLastTopLevelChildText(nodeBody, 'Key');
    if (!key) continue;
    const propertyName = findLastTopLevelChildText(nodeBody, 'PropertyName') ?? key;
    const fieldName = findLastTopLevelChildText(nodeBody, 'FieldName') ?? key.toUpperCase();
    const name = findLastTopLevelChildText(nodeBody, 'Name') ?? key;
    const app = appearanceByKey.get(key);
    fields.push({
      key,
      type: 'text',
      caption: app?.caption ?? name,
      propertyName,
      fieldName,
      container: app?.container
    });
  }
  return fields;
}

function collectAppearances(
  xml: string,
  out: Map<string, { caption?: string; container?: string }>
): void {
  type Frame = { tag: string; bodyStart: number; isTextFieldAppearance: boolean };
  const stack: Frame[] = [];
  for (const tk of iterateTagTokens(xml)) {
    if (tk.isSelfClose) continue;
    if (!tk.isClose) {
      stack.push({
        tag: tk.tag,
        bodyStart: tk.end,
        isTextFieldAppearance: tk.tag === 'TextFieldAppearance'
      });
      continue;
    }
    const frame = stack.pop();
    if (!frame || !frame.isTextFieldAppearance) continue;
    const body = xml.substring(frame.bodyStart, tk.start);
    const key = findLastTopLevelChildText(body, 'Key');
    if (!key) continue;
    out.set(key, {
      caption: findLastTopLevelChildText(body, 'Caption'),
      container: findLastTopLevelChildText(body, 'Container')
    });
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
pnpm vitest run tests/erp/bos-xml.test.ts -t parseFieldsFromKernelXml
```
Expected: 4 passed。

- [ ] **Step 5: 跑全 bos-xml 测试 + typecheck**

```bash
pnpm vitest run tests/erp/bos-xml.test.ts
pnpm typecheck
```
Expected: 全 pass(没破坏既有 plugin 解析)。

- [ ] **Step 6: 提交**

```bash
git add src/main/erp/k3cloud/bos-xml.ts tests/erp/bos-xml.test.ts
git commit -m "feat(bos-xml): 解析扩展 FKERNELXML 中的 TextField 字段定义"
```

---

## Task 5: bos-writer.ts 加 listExtensionFields(读 DB → 调 parser)

**Files:**
- Modify: `src/main/erp/k3cloud/bos-writer.ts`(在 `addFieldToExtension` 函数之前/之后加导出)
- Modify: `tests/erp/bos-writer.test.ts`

- [ ] **Step 1: 找现有测试模式 → 写失败测试**

先看 `tests/erp/bos-writer.test.ts` 里 mock pool 的方式(已有 `listExtensions` / `listFormPlugins` 可参考)。在文件末尾追加:

```ts
import { listExtensionFields } from '../../src/main/erp/k3cloud/bos-writer';

describe('listExtensionFields', () => {
  it('returns [] when extension row missing', async () => {
    const pool = mockPool({ recordset: [] });
    const fields = await listExtensionFields(pool, EXT_ID);
    expect(fields).toEqual([]);
  });

  it('returns [] when FKERNELXML is null', async () => {
    const pool = mockPool({ recordset: [{ xml: null }] });
    const fields = await listExtensionFields(pool, EXT_ID);
    expect(fields).toEqual([]);
  });

  it('parses fields from FKERNELXML round-trip', async () => {
    const xml = insertTextFieldIntoKernelXml(
      buildExtensionKernelXml(EXT_ID, []),
      { spec: { key: 'F_DEMO', caption: '演示字段' } }
    );
    const pool = mockPool({ recordset: [{ xml }] });
    const fields = await listExtensionFields(pool, EXT_ID);
    expect(fields.map((f) => f.key)).toEqual(['F_DEMO']);
    expect(fields[0].caption).toBe('演示字段');
  });
});
```

(测试需要导入 `buildExtensionKernelXml` / `insertTextFieldIntoKernelXml` / 已有的 `EXT_ID` const / `mockPool` helper —— 复用文件顶部已有的。如顶部没有从 bos-xml import 这两个,就在文件顶 import 块里加上。)

- [ ] **Step 2: 跑测试确认失败**

```bash
pnpm vitest run tests/erp/bos-writer.test.ts -t listExtensionFields
```
Expected: FAIL `listExtensionFields is not exported`。

- [ ] **Step 3: 实现**

在 `src/main/erp/k3cloud/bos-writer.ts`,先把 import 区(文件顶 line 20-27)加上 `parseFieldsFromKernelXml` + `ExtensionFieldMeta`:

```ts
import {
  addPluginToKernelXml,
  buildExtensionKernelXml,
  insertTextFieldIntoKernelXml,
  parseFieldsFromKernelXml,
  parseFormPluginsFromKernelXml,
  removePluginFromKernelXml,
  type ExtensionFieldMeta,
  type TextFieldSpec
} from './bos-xml';
```

在 `addFieldToExtension` 函数之前(line 261 附近)插入:

```ts
// ─── listExtensionFields ───────────────────────────────────────────────

/**
 * 读扩展的 FKERNELXML, 解析出已有扩展字段(目前只识别 TextField)。
 * 写入工具(`addFieldToExtension`)的反查闭环 —— agent 调 add_field 之后
 * 必须用这个验证字段真的进了 XML, 而不是骗自己用查原厂字段的
 * `kingdee_get_fields`(那个工具看不到扩展字段)。
 */
export async function listExtensionFields(
  pool: sql.ConnectionPool,
  extId: string
): Promise<ExtensionFieldMeta[]> {
  requireValid(GET_KERNEL_XML_SQL);
  const r = await pool
    .request()
    .input('id', sql.VarChar(64), extId)
    .query<{ xml: string | null }>(GET_KERNEL_XML_SQL);
  const xml = r.recordset[0]?.xml;
  if (!xml) return [];
  return parseFieldsFromKernelXml(xml);
}
```

- [ ] **Step 4: 跑测试 + typecheck**

```bash
pnpm vitest run tests/erp/bos-writer.test.ts
pnpm typecheck
```
Expected: 全 pass。

- [ ] **Step 5: 提交**

```bash
git add src/main/erp/k3cloud/bos-writer.ts tests/erp/bos-writer.test.ts
git commit -m "feat(bos-writer): listExtensionFields 读扩展字段定义"
```

---

## Task 6: kingdee_get_extension_fields 工具 + 闭环规则

**Files:**
- Modify: `src/main/agent/bos-write-tools.ts`(import + 工具定义 + buildBosWriteTools 注册)
- Modify: `src/main/agent/prompts/erp-rules/k3cloud.md`(在闭环清单里加扩展字段反查 + 把 `kingdee_get_fields` 是 "原厂字段" 写明)
- Modify: `tests/agent/k3cloud-tools.test.ts`

- [ ] **Step 1: 写工具集成测试(失败)**

在 `tests/agent/k3cloud-tools.test.ts` 末尾(找现有 `kingdee_add_field` 测试附近)追加 describe:

```ts
describe('kingdee_get_extension_fields tool', () => {
  it('exists in buildBosWriteTools when project active', () => {
    const tools = buildBosWriteTools(mockConnector(), 'p_test');
    const t = tools.find((x) => x.definition.name === 'kingdee_get_extension_fields');
    expect(t).toBeDefined();
    expect(t?.parallelSafe).toBe(true);
  });

  it('returns parsed fields as JSON', async () => {
    const xml = insertTextFieldIntoKernelXml(
      buildExtensionKernelXml(EXT_ID, []),
      { spec: { key: 'F_DEMO', caption: '演示字段' } }
    );
    const conn = mockConnector({
      kernelXml: { [EXT_ID]: xml }
    });
    const tools = buildBosWriteTools(conn, 'p_test');
    const t = tools.find((x) => x.definition.name === 'kingdee_get_extension_fields')!;
    const out = await t.execute({ extId: EXT_ID });
    const parsed = JSON.parse(out);
    expect(parsed.count).toBe(1);
    expect(parsed.fields[0].key).toBe('F_DEMO');
    expect(parsed.fields[0].caption).toBe('演示字段');
  });
});
```

(`mockConnector` / `EXT_ID` / 字段方法已经在文件里有就复用;若 `mockConnector` 还不支持 `kernelXml`,先扩展它的 interface。如果不会扩,先把测试简化为只测 "工具存在" + parallelSafe,实施时再补 execute 测试。)

- [ ] **Step 2: 跑测试确认失败**

```bash
pnpm vitest run tests/agent/k3cloud-tools.test.ts -t kingdee_get_extension_fields
```
Expected: FAIL `kingdee_get_extension_fields not found`。

- [ ] **Step 3: 在 bos-write-tools.ts 加工具**

`src/main/agent/bos-write-tools.ts` 顶部 import(line 17-26)加 `listExtensionFields`:

```ts
import {
  addFieldToExtension,
  createExtensionWithPythonPlugin,
  deleteExtension,
  listExtensionFields,
  listExtensions,
  listFormPlugins,
  probeBosEnvironment,
  registerPythonPluginOnExtension,
  unregisterPlugin
} from '../erp/k3cloud/bos-writer';
```

在 `buildBosWriteTools` 返回数组(line 46-55)里增加一项:

```ts
return [
  listExtensionsTool(c),
  listFormPluginsTool(c),
  listExtensionFieldsTool(c),
  probeBosEnvironmentTool(c),
  createExtensionTool(c, pid),
  registerPluginTool(c, pid),
  unregisterPluginTool(c, pid),
  deleteExtensionTool(c, pid),
  addFieldTool(c, pid)
];
```

在 `listFormPluginsTool` 函数之后(line 134 附近)插入:

```ts
function listExtensionFieldsTool(c: K3CloudConnector): ToolHandler {
  return {
    parallelSafe: true,
    definition: {
      name: 'kingdee_get_extension_fields',
      description:
        '列出某扩展上已有的扩展字段 (parse FKERNELXML 中的 TextField 节点)。注意:这只看扩展字段,不看父对象的原厂字段——查原厂字段用 kingdee_get_fields。新加扩展字段后必用这个反查,不要用 kingdee_get_fields 验证扩展字段(那是查原厂的)。',
      parameters: {
        type: 'object',
        properties: {
          extId: { type: 'string', description: '扩展 GUID。' }
        },
        required: ['extId']
      }
    },
    async execute(args) {
      const extId = String(args.extId);
      const pool = await c.getPool();
      const fields = await listExtensionFields(pool, extId);
      return JSON.stringify({ count: fields.length, fields }, null, 2);
    }
  };
}
```

- [ ] **Step 4: 更新 erp-rules prompt**

`src/main/agent/prompts/erp-rules/k3cloud.md`:

(a) line 7 把工具名加进只读清单:

```diff
- - 元数据**只读**:`kingdee_list_objects` / `kingdee_get_object` / `kingdee_get_fields` / `kingdee_list_subsystems` / `kingdee_search_metadata` / `kingdee_list_extensions` / `kingdee_list_form_plugins` / `kingdee_probe_bos_environment`
+ - 元数据**只读**:`kingdee_list_objects` / `kingdee_get_object` / `kingdee_get_fields` (**只查父对象原厂字段**) / `kingdee_get_extension_fields` (**只查扩展字段**) / `kingdee_list_subsystems` / `kingdee_search_metadata` / `kingdee_list_extensions` / `kingdee_list_form_plugins` / `kingdee_probe_bos_environment`
```

(b) line 8 把写入工具补 add_field:

```diff
- - BOS 扩展 + 插件**写入**:`kingdee_create_extension_with_python_plugin`(新扩展)、`kingdee_register_python_plugin`(已有扩展)
+ - BOS 扩展 + 插件**写入**:`kingdee_create_extension_with_python_plugin`(新扩展)、`kingdee_register_python_plugin`(已有扩展)、`kingdee_add_field`(扩展字段)
```

(c) "写入后的闭环" 那一节(line 53-61)在 1) 2) 之间补 1.5),并明确扩展字段反查必须用对应工具:

```diff
1. **反查扩展落库**:`kingdee_list_extensions <parentFormId>` 里应该能看到新 FID + 名称
+ 1.5. **反查扩展字段**(若调用了 `kingdee_add_field`):`kingdee_get_extension_fields <extId>` 必须能看到新 key 和 caption。**不要用 `kingdee_get_fields` 验证扩展字段——那个工具只看父对象的原厂字段,扩展字段永远查不到,它返回空不代表写入失败。**
2. **反查插件挂载**:`kingdee_list_form_plugins <extId>` 里应该能看到 `className` 对得上、`type=python`、`pyScript` 不为空
```

- [ ] **Step 5: 跑测试 + typecheck**

```bash
pnpm vitest run tests/agent/k3cloud-tools.test.ts
pnpm typecheck
```
Expected: 全 pass。

- [ ] **Step 6: 提交**

```bash
git add src/main/agent/bos-write-tools.ts \
  src/main/agent/prompts/erp-rules/k3cloud.md \
  tests/agent/k3cloud-tools.test.ts
git commit -m "feat(agent): kingdee_get_extension_fields 工具 + add_field 闭环规则"
```

---

## Task 7: kingdee_add_field 默认放置改进(避免重叠)

**问题**:`bos-xml.ts:254` 的 `defaultNumericGenerator` 给出 `tabindex: 9999, listTabIndex: 9999, zOrderIndex: 99`,但 `bos-xml.ts:294-295` 把 Top/Left 写死成 `<Top>10</Top><Left>10</Left>`,导致新字段叠在容器左上角和现有字段重叠。

**v0.1 务实策略**:不去 parse 父布局推算空位(父 form FKERNELXML 1MB+ 复杂),改成把默认 `Top` 改大(9000)+ `ZOrderIndex` 改大(9999),让字段视觉上在容器底部不和现有字段重叠;tool reminder 文案明示"在 BOS Designer 拖到合适位置"。同时把 `top` / `left` 暴露为可选参数,agent 真有需要时可显式指定。

**Files:**
- Modify: `src/main/erp/k3cloud/bos-xml.ts`(`TextFieldSpec` 加 `top` / `left`,`renderTextFieldAppearanceNode` 用上,`defaultNumericGenerator` 注释更新)
- Modify: `src/main/agent/bos-write-tools.ts`(`kingdee_add_field` 加 top/left 参数 + reminder 文案)
- Modify: `tests/erp/bos-xml.test.ts`

- [ ] **Step 1: 写失败测试**

在 `tests/erp/bos-xml.test.ts` 找 `insertTextFieldIntoKernelXml` 的 describe(若已存在);若没有,在末尾追加:

```ts
describe('insertTextFieldIntoKernelXml default placement', () => {
  it('places new field with high Top/ZOrder so it does not overlap top-left', () => {
    const xml = insertTextFieldIntoKernelXml(
      buildExtensionKernelXml(EXT, []),
      { spec: { key: 'F_DEMO', caption: '演示字段' } }
    );
    expect(xml).toContain('<Top>9000</Top>');
    expect(xml).toContain('<ZOrderIndex>9999</ZOrderIndex>');
  });

  it('respects explicit top/left from spec', () => {
    const xml = insertTextFieldIntoKernelXml(
      buildExtensionKernelXml(EXT, []),
      { spec: { key: 'F_X', caption: '甲', top: 200, left: 50 } }
    );
    expect(xml).toContain('<Top>200</Top>');
    expect(xml).toContain('<Left>50</Left>');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
pnpm vitest run tests/erp/bos-xml.test.ts -t "default placement"
```
Expected: FAIL(当前 `Top=10`,且 `top` / `left` 不在 spec interface 上 → typecheck 也会爆)。

- [ ] **Step 3: 改 bos-xml.ts**

`src/main/erp/k3cloud/bos-xml.ts` 第 222-240 行 `TextFieldSpec`:

```diff
 export interface TextFieldSpec {
   key: string;
   caption: string;
   name?: string;
   propertyName?: string;
   fieldName?: string;
   containerKey?: string;
   width?: number;
   labelWidth?: number;
+  /** 字段在容器中的 Top 像素位置,默认 9000(底部),避免和现有字段重叠。*/
+  top?: number;
+  /** 字段在容器中的 Left 像素位置,默认 10。*/
+  left?: number;
 }
```

`renderTextFieldAppearanceNode`(line 277-304)用上参数:

```diff
 function renderTextFieldAppearanceNode(
   spec: TextFieldSpec,
   id: string,
   zOrderIndex: number,
   tabindex: number
 ): string {
   const container = spec.containerKey ?? 'FTAB_P0';
   const width = spec.width ?? 300;
   const labelWidth = spec.labelWidth ?? 100;
+  const top = spec.top ?? 9000;
+  const left = spec.left ?? 10;
   return (
     '<TextFieldAppearance ElementType="1" ElementStyle="1">' +
     '<EmptyText action="setnull"/>' +
     `<Key>${xmlEscape(spec.key)}</Key>` +
     '<ListDefaultWidth>100</ListDefaultWidth>' +
     `<Container>${xmlEscape(container)}</Container>` +
     `<ZOrderIndex>${zOrderIndex}</ZOrderIndex>` +
     `<Tabindex>${tabindex}</Tabindex>` +
-    '<Left>10</Left>' +
-    '<Top>10</Top>' +
+    `<Left>${left}</Left>` +
+    `<Top>${top}</Top>` +
     `<LabelWidth>${labelWidth}</LabelWidth>` +
     `<Width>${width}</Width>` +
     '<Visible>1023</Visible>' +
     '<VisibleExt>100</VisibleExt>' +
     `<Caption>${xmlEscape(spec.caption)}</Caption>` +
     `<Id>${xmlEscape(id)}</Id>` +
     '</TextFieldAppearance>'
   );
 }
```

- [ ] **Step 4: 在 add_field 工具暴露 top/left + 改 reminder**

`src/main/agent/bos-write-tools.ts` `addFieldTool` (line 285-358):

(a) `parameters.properties` 加两个可选参数:

```diff
           containerKey: {
             type: 'string',
             description: '(可选) 放在哪个布局容器, 默认 "FTAB_P0" (主页签)。'
+          },
+          top: {
+            type: 'number',
+            description: '(可选) 字段 Top 像素, 默认 9000 (容器底部, 不和现有字段重叠)。'
+          },
+          left: {
+            type: 'number',
+            description: '(可选) 字段 Left 像素, 默认 10。'
           }
         },
         required: ['extId', 'type', 'key', 'caption']
       }
     },
```

(b) `execute` 里把 top/left 传进 spec:

```diff
       const r = await addFieldToExtension(pool, projectId, String(args.extId), 'text', {
         key: String(args.key),
         caption: String(args.caption),
         name: args.name !== undefined ? String(args.name) : undefined,
         propertyName: args.propertyName !== undefined ? String(args.propertyName) : undefined,
         fieldName: args.fieldName !== undefined ? String(args.fieldName) : undefined,
-        containerKey: args.containerKey !== undefined ? String(args.containerKey) : undefined
+        containerKey: args.containerKey !== undefined ? String(args.containerKey) : undefined,
+        top: args.top !== undefined ? Number(args.top) : undefined,
+        left: args.left !== undefined ? Number(args.left) : undefined
       });
```

(c) reminder 文案补"位置在容器底部需用户拖":

```diff
           reminder:
-            '字段已写入 DB。去 BOS Designer 中刷新扩展(工具栏刷新按钮)就能看到新字段 —— 位次 / 坐标 Designer 会自动 normalize。如用 SVN 同步共享给团队, 记得点一次"同步"。'
+            '字段已写入 DB。去 BOS Designer 中刷新扩展(工具栏刷新按钮)就能看到新字段。**字段默认落在容器底部(Top=9000),需在 BOS Designer 拖动到合适位置。**如用 SVN 同步共享给团队, 记得点一次"同步"。'
```

- [ ] **Step 5: 跑测试 + typecheck**

```bash
pnpm vitest run tests/erp/bos-xml.test.ts
pnpm typecheck
```
Expected: 新加的 2 测试 pass + 既有测试不破坏 + typecheck 干净。

- [ ] **Step 6: 提交**

```bash
git add src/main/erp/k3cloud/bos-xml.ts src/main/agent/bos-write-tools.ts \
  tests/erp/bos-xml.test.ts
git commit -m "fix(bos): 新增字段默认放容器底部, 暴露 top/left 参数"
```

---

## 全量回归 + 收尾

- [ ] **Step 8.1: 跑全部测试**

```bash
pnpm test
```
Expected: 全部 pass(原 31 文件 / 223 测试 + 新增 ~12 测试)。

- [ ] **Step 8.2: typecheck + lint**

```bash
pnpm typecheck && pnpm lint
```
Expected: 0 错误。

- [ ] **Step 8.3: 启动 dev 端到端验证**

```bash
pnpm dev
```

在对话里复跑 demo 场景:"给销售订单加一个 F_DEMO 文本字段,挂到已有扩展上"。验证:
- 流式开始有"思考中…",工具运行时变 "执行 kingdee_xxx 中…",text 流入时切回 cursor
- 表格列宽对齐(顾问演示常见的"项 / 内容"对照表)
- 写入后 agent 不再说"按 F5",而是"在 BOS Designer 中刷新"
- 写入后 agent 用 `kingdee_get_extension_fields` 反查,能看到 F_DEMO + caption,不再用 `kingdee_get_fields` 假装验证
- 去客户 BOS Designer 看新字段(刷新后),应该在容器底部不和现有字段重叠

- [ ] **Step 8.4: 更新 CLAUDE.md 进度行**

`CLAUDE.md` 在 Plan 进度清单里加一项:

```diff
   - ✅ **Plan 5.8(非正式)—— Skills 内容大扩张(2026-04-23)**
+  - ✅ **Plan 5.9(非正式)—— UX 修补 + 扩展字段反查 + 字段布局放置(2026-04-25)**
+    - UI:流式 / 工具运行均显式 thinking-style 指示;markdown 表格列宽对齐
+    - Prompt:BOS 刷新方式不写死 F5 改通用表述
+    - 工具:`kingdee_get_extension_fields` 解析 FKERNELXML 反查扩展字段;system prompt 闭环规则把"扩展字段反查"和"原厂字段反查"分清楚
+    - `kingdee_add_field`:默认 Top=9000 / ZOrder=9999 落容器底部避重叠,top/left 暴露为可选参数
   - ⏳ Plan 6 — 打包发布 + v0.1 Alpha Release
```

- [ ] **Step 8.5: 单提交 docs**

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md 标注 Plan 5.9 完成"
```

---

## 自检

**Spec 覆盖**:
1. UI 思考/工具反馈 → Task 1 ✓
2. 表格对齐 → Task 2 ✓
3. F5 → 通用刷新 → Task 3 ✓
4. 扩展字段反查工具 → Tasks 4-6 ✓
5. 字段布局放置 → Task 7 ✓

**Placeholder 扫描**:无 TBD / "implement later" / "类似 Task N" / 抽象 "处理边界条件"。

**类型一致性**:`ExtensionFieldMeta`(Task 4 定义)在 Task 5 / Task 6 一致使用;`derivePendingActivity` 返回类型 `PendingActivity | null` 在 Task 1 内部使用,无外溢。

**遗留**:
- 真正的"读父对象布局自动放置"(替代 Task 7 的 Top=9000 hack)留到后续 cycle,目前用户痛点已经缓解(不重叠)
- `kingdee_get_extension_fields` 目前只解析 TextField,后续支持 number/date 等字段类型时,只需要在 `parseFieldsFromKernelXml` 里加节点识别分支,工具签名不变
