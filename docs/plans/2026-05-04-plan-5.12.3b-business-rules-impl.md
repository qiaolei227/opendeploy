# Plan 5.12.3b — 业务规则实施 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实施业务规则 .NET bridge ops + agent 工具 + Calculate AST validator + e2e demo，让 agent 能在 BOS 扩展上加 Calculate 字段级 UpdateAction 和 GetInvStock 实体级规则、列出已有规则、删除规则。

**Architecture:**
- 严格沿用 Plan 5.12.4 v2 已落地的 **patch-only bridge** pattern：bridge ops 是纯函数 `string xml in → string xml out`（或 `→ summary out`），**不持 HTTP session、不与 K/3 server 通信**
- I/O 留在 TS 端（与 5.12.4 v2 `patchExtXml` 同形）：connector 用现有 `connector.getKernelXml(extId)` 拉扩展当前 FKERNELXML → 调 bridge patch op 改 → 用现有 `SaveForIDEV9` HTTP wrapper 推回。具体写回路径参考 `src/main/agent/bos-rpc-tools.ts` 中 `loadExtensionForSave` + 其后的 SaveForIDEV9 调用（已在 add-fields / create-entry 等工具落地）
- bridge `BosContext` 用 `PatchMeta` 风格：`DcxmlSerializer.DeserializeFromString(formMetadataXml)` → 拿到强类型对象 → 在 BusinessInfo 集合上加/删 EntityServiceRule / UpdateAction → `SerializeToString` 吐回 element-level delta DCXML
- LLM 输入走 typed schema（参见 `docs/recon/2026-05-04-business-rules-tier-b.md` §4.1）
- Calculate 工具内部跑 IronPython AST 校验 + 1 次 retry（route C）
- GetInvStock 工具走字段引用查 schema 校验

**Tech Stack:** TypeScript + C# .NET 4.8 + Vitest + Kingdee.BOS.Core 反射

**Scope:** 仅 v0.1 锁定的 2 ActionId（Calculate=2 + GetInvStock=67）。其余 4 个 ActionId（3/23/42/70）留 v0.2。

**依赖:** Plan 5.12.3a 已完成（commits `8a791ce` `20af7c8` `f9187c1`）

---

## File Structure

| 文件 | Create / Modify |
|---|---|
| `bos-bridge/BosContext.cs` | Modify: 加 4 个业务规则 patch ops（PatchBusinessInfo helper + xml-in/xml-out / xml-in/summary-out）|
| `bos-bridge/Program.cs` | Modify: dispatch 4 个新 op |
| `src/main/erp/k3cloud/connector.ts` | Modify: 加 4 个 wrapper 方法（`getKernelXml` 拉 → bridge patch → `SaveForIDEV9` 推）|
| `src/main/agent/business-rule-tools.ts` | Create: 5 个 agent 工具 |
| `src/main/agent/k3cloud-tools.ts` | Modify: 注册 business-rule-tools |
| `src/main/agent/validators/ironpython-ast.ts` | Create: IronPython 2.7 子集 AST parser（用 acorn-style approach 或简版）|
| `src/main/agent/validators/calculate-rule.ts` | Create: Calculate 表达式校验器（AST + 字段引用 + 函数白名单）|
| `src/main/agent/validators/field-existence.ts` | Create: 通用 field-key 查 schema 校验器 |
| `tests/erp/bridge/business-rules.test.ts` | Create: bridge ops 集成测试 |
| `tests/agent/business-rule-tools.test.ts` | Create: agent 工具单测 |
| `tests/agent/validators/calculate-rule.test.ts` | Create: validator 单测 |
| `tests/agent/validators/ironpython-ast.test.ts` | Create: AST parser 单测 |
| `src/main/agent/prompts/erp-rules/k3cloud.md` | Modify: 加业务规则使用规则段 |

---

## Phase 2 — .NET Bridge Ops（Task 2.1 ~ 2.4）

### Task 2.1: bridge op `list_business_rules`

**Files:**
- Modify: `bos-bridge/BosContext.cs`
- Modify: `bos-bridge/Program.cs`
- Modify: `tests/erp/bridge/integration.test.ts`（沿用现有 `describeIfBridge` skip-if-no-bridge 模式，加新 describe 块）
- Create: 2 个 fixture FKERNELXML 文件，路径同 `src/main/erp/k3cloud/rpc/baselines/`（业务规则相关 baseline 单独子目录或 `business-rules-*.xml` 前缀，由 implementer 选）

**Why first:** 只读 op，验证 bridge 能正确把 form metadata XML 反序列化到 BusinessInfo 并 walk 出规则。后续 add/remove 也走 deserialize → modify → serialize 同一路径，先把 walk 跑通。

- [ ] **Step 1: 准备 fixture FKERNELXML**

从 Plan 5.12.3a 实证 capture（`.scratch/captures/decoded/business-rules/req-120/ap0.source.xml`）拷贝/裁出：
- `business-rules-with-rules.xml` — 含 1 条 GetInvStock 实体规则 + 1 条 Calculate UpdateAction 的最小 form metadata XML
- `business-rules-no-rules.xml` — 同源裁出无业务规则版本

具体放置目录由 implementer 决定（参考现有 `src/main/erp/k3cloud/rpc/baselines/sale-order-outstock-origin.xml` 模式）。

- [ ] **Step 2: 写 bridge 测试**

参考 `tests/erp/bridge/integration.test.ts` 现有结构，加新 describe block。**直接复用** 现有 `BridgeClient` + `describeIfBridge` skip-if-no-bridge 模式：

```typescript
// 在 integration.test.ts 内追加（或新建 tests/erp/bridge/business-rules.test.ts 复用同 BridgeClient 引导逻辑）：

describeIfBridge('bos-bridge: list_business_rules', () => {
  // ... 复用 client / beforeAll / afterAll 同 integration.test.ts

  it('returns empty arrays for form with no rules', async () => {
    const xml = readFileSync('<fixture-dir>/business-rules-no-rules.xml', 'utf8');
    const result = await client.send<{ entityRules: any[]; fieldUpdateActions: any[] }>('list_business_rules', { xml });
    expect(result.entityRules).toEqual([]);
    expect(result.fieldUpdateActions).toEqual([]);
  });

  it('finds GetInvStock entity rule + Calculate UpdateAction in fixture', async () => {
    const xml = readFileSync('<fixture-dir>/business-rules-with-rules.xml', 'utf8');
    const result = await client.send<{ entityRules: any[]; fieldUpdateActions: any[] }>('list_business_rules', { xml });
    expect(result.entityRules).toHaveLength(1);
    expect(result.entityRules[0]).toMatchObject({
      preCondition: expect.stringContaining("'01.01'"),
      services: expect.arrayContaining([
        expect.objectContaining({ actionId: 67, className: 'GetInvStockBusinessServiceMeta' }),
      ]),
    });
    expect(result.fieldUpdateActions).toHaveLength(1);
    expect(result.fieldUpdateActions[0]).toMatchObject({ actionId: 2, className: 'FormBusinessService' });
  });
});
```

- [ ] **Step 3: 验证测试失败**

Run: `pnpm test tests/erp/bridge/`
Expected: FAIL — `list_business_rules` op 不存在

- [ ] **Step 4: bridge C# 端实现 op**

Edit `bos-bridge/BosContext.cs` — 加 `ListBusinessRules(string xml)`，沿用 `PatchMeta` 的 `_serializer.DeserializeFromString` 路径：

```csharp
public ListBusinessRulesResult ListBusinessRules(string xml)
{
    if (string.IsNullOrEmpty(xml)) throw new ArgumentException("xml is empty", nameof(xml));
    var formMeta = _serializer.DeserializeFromString(xml);
    if (formMeta == null) throw new InvalidOperationException("DeserializeFromString returned null");

    // formMeta 实际类型是 FormMetadata（Kingdee.BOS.Core.Metadata.FormElement.FormMetadata）
    // 含 BusinessInfo（持 HeadEntity / SubHeadEntities / EntryEntities + Fields）。
    // 先反射拿 BusinessInfo，避免硬依赖（bridge 当前只引用 Core）；类名稳定的话也可直接 cast。

    var entityRules = new List<EntityRuleSummary>();
    var fieldActions = new List<FieldUpdateActionSummary>();

    foreach (var entity in EnumerateEntities(formMeta))
    {
        var rules = (IList?)entity.GetType().GetProperty("EntityServiceRules")?.GetValue(entity);
        if (rules == null) continue;
        foreach (var rule in rules)
        {
            entityRules.Add(BuildEntityRuleSummary(rule, entity));
        }
    }

    foreach (var field in EnumerateFields(formMeta))
    {
        var actions = (IList?)field.GetType().GetProperty("UpdateActions")?.GetValue(field);
        if (actions == null || actions.Count == 0) continue;
        foreach (var svc in actions)
        {
            fieldActions.Add(BuildFieldActionSummary(svc, field));
        }
    }

    return new ListBusinessRulesResult { EntityRules = entityRules, FieldUpdateActions = fieldActions };
}
```

`EnumerateEntities` / `EnumerateFields` / `BuildEntityRuleSummary` / `BuildFieldActionSummary` 是新 helper，放在 BosContext 私有。具体反射路径（哪个属性是 BusinessInfo，BusinessInfo 上哪个属性是 Entities，FormBusinessService 上哪些属性要 dump）由 implementer 在 spike 时用 ilspycmd dump `Kingdee.BOS.Core.Metadata.FormElement.FormMetadata` 决定。

Edit `bos-bridge/Program.cs` 在 `Dispatch` 加 case：

```csharp
case "list_business_rules":
    return ctx.ListBusinessRules(RequireString(req, "xml"));
```

- [ ] **Step 5: build bridge**

```bash
cd bos-bridge && dotnet build -c Release
```

Expected: Build succeeded.

- [ ] **Step 6: 运行测试 → PASS**

Run: `pnpm test tests/erp/bridge/`
Expected: 2/2 PASS（list_business_rules 新增的 2 个 case）+ 现有 normalize/add_convert_field_map 等 case 无回归。

- [ ] **Step 7: Commit**

```bash
git add bos-bridge/ tests/erp/bridge/integration.test.ts <fixtures>
git commit -m "feat(bridge): list_business_rules op (Plan 5.12.3b)"
```

---

### Task 2.2: bridge op `add_entity_service_rule`

**Files:**
- Modify: `bos-bridge/BosContext.cs`
- Modify: `bos-bridge/Program.cs`
- Modify: `tests/erp/bridge/integration.test.ts`（同 Task 2.1 加 describe）

- [ ] **Step 1: 写测试 — 在 no-rules fixture 上加一条 GetInvStock，调 list 验证规则在**

```typescript
describeIfBridge('bos-bridge: add_entity_service_rule', () => {
  // ... 同 Task 2.1 的 client 引导

  it('adds GetInvStock entity rule, list returns it', async () => {
    const inputXml = readFileSync('<fixture-dir>/business-rules-no-rules.xml', 'utf8');
    const ruleId = '11111111-1111-1111-1111-111111111111';

    const { xml: patchedXml } = await client.send<{ xml: string }>('add_entity_service_rule', {
      xml: inputXml,
      ruleId,
      description: 'TS test - GetInvStock',
      preCondition: " FBillTypeID.FNumber = '01.01'",
      preConditionDesc: 'test',
      entityKey: 'FBillHead',  // 默认 HeadEntity
      services: [{
        className: 'GetInvStockBusinessServiceMeta',
        actionId: 67,
        properties: { stockQtyField: 'F_TestQty' },
      }],
    });

    // round-trip：调 list 验证 patched XML 含新规则
    const listed = await client.send<{ entityRules: any[] }>('list_business_rules', { xml: patchedXml });
    expect(listed.entityRules.find((r: any) => r.id === ruleId)).toMatchObject({
      preCondition: expect.stringContaining("'01.01'"),
      services: expect.arrayContaining([
        expect.objectContaining({ actionId: 67, className: 'GetInvStockBusinessServiceMeta' }),
      ]),
    });
  });
});
```

- [ ] **Step 2: 验证测试失败**

Run: `pnpm test tests/erp/bridge/`
Expected: FAIL — `add_entity_service_rule` op 不存在

- [ ] **Step 3: bridge C# 端实现 op**

加到 `BosContext.cs`，沿用 `PatchMeta` 风格——deserialize → modify → serialize 都在 BOS Core 反射里完成，bridge 不直接写 XML 字符串：

```csharp
public string AddEntityServiceRule(string xml, AddEntityServiceRuleArgs args)
{
    if (string.IsNullOrEmpty(xml)) throw new ArgumentException("xml is empty", nameof(xml));
    if (string.IsNullOrEmpty(args.RuleId)) throw new ArgumentException("ruleId is empty");
    if (string.IsNullOrEmpty(args.PreCondition?.Trim()))
        throw new ArgumentException("preCondition must be non-empty (BOS enforces this on EntityServiceRule)");

    var formMeta = _serializer.DeserializeFromString(xml)
        ?? throw new InvalidOperationException("DeserializeFromString returned null");

    var entity = FindEntity(formMeta, args.EntityKey);  // null/empty → HeadEntity
    var rulesList = GetOrCreateEntityServiceRules(entity);

    var ruleType = ResolveType("Kingdee.BOS.Core.Metadata.BusinessService.EntityServiceRule");
    var rule = Activator.CreateInstance(ruleType)!;
    SetProp(rule, "Id", args.RuleId);
    SetProp(rule, "Description", args.Description);
    SetProp(rule, "PreCondition", args.PreCondition);
    SetProp(rule, "PreConditionDesc", args.PreConditionDesc);
    SetProp(rule, "Seq", rulesList.Count + 1);

    var servicesProp = ruleType.GetProperty("WhenTrueBusinessServices")!;
    var servicesList = servicesProp.GetValue(rule) as IList
        ?? (IList)Activator.CreateInstance(servicesProp.PropertyType)!;
    servicesProp.SetValue(rule, servicesList);

    foreach (var svc in args.Services)
    {
        var svcType = ResolveServiceMetaType(svc.ClassName);
        var instance = Activator.CreateInstance(svcType)!;
        SetProp(instance, "Id", Guid.NewGuid().ToString("N"));
        if (svc.Properties != null)
        {
            foreach (var kv in svc.Properties)
            {
                var prop = svcType.GetProperty(kv.Key, BindingFlags.Public | BindingFlags.Instance);
                if (prop != null && prop.CanWrite) prop.SetValue(instance, ConvertValue(kv.Value, prop.PropertyType));
            }
        }
        servicesList.Add(instance);
    }

    rulesList.Add(rule);
    return _serializer.SerializeToString(formMeta, null);
}
```

`ResolveServiceMetaType` 一次性建索引（bridge 启动时跑），按短类名 lookup（避开命名空间漂移）：

```csharp
private static readonly Lazy<Dictionary<string, Type>> ServiceMetaTypes = new Lazy<Dictionary<string, Type>>(() =>
{
    var baseType = ResolveType("Kingdee.BOS.Core.Metadata.BusinessService.FormBusinessService");
    return baseType.Assembly.GetTypes()
        .Where(t => baseType.IsAssignableFrom(t) && !t.IsAbstract)
        .ToDictionary(t => t.Name);
});
```

Edit `Program.cs` Dispatch 加 case `"add_entity_service_rule"` — 解出 args、调 `ctx.AddEntityServiceRule(xml, args)`，包成 `new { xml = ... }` 返回。

- [ ] **Step 4: Build + run test**

```bash
cd bos-bridge && dotnet build -c Release
cd .. && pnpm test tests/erp/bridge/
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add bos-bridge/ tests/erp/bridge/integration.test.ts
git commit -m "feat(bridge): add_entity_service_rule op for GetInvStock + base FormBusinessService (Plan 5.12.3b)"
```

---

### Task 2.3: bridge op `add_field_update_action`

**Files:**
- Modify: `bos-bridge/BosContext.cs`
- Modify: `bos-bridge/Program.cs`
- Modify: `tests/erp/bridge/integration.test.ts`

- [ ] **Step 1: 写测试 — 在 no-rules fixture 上某 field 加 Calculate UpdateAction，list 验证**

```typescript
describeIfBridge('bos-bridge: add_field_update_action', () => {
  it('adds Calculate UpdateAction to a Field', async () => {
    const inputXml = readFileSync('<fixture-dir>/business-rules-no-rules.xml', 'utf8');

    const { xml: patchedXml } = await client.send<{ xml: string }>('add_field_update_action', {
      xml: inputXml,
      fieldKey: 'F_TestInt',  // 必须是 fixture 中已存在的字段 key
      services: [{
        className: 'FormBusinessService',  // 基类（Calculate 用）
        actionId: 2,
        parameters: [' F_TestDecimal = F_TestInt * 2 '],
      }],
      disabledEvents: ['RaiseValueChanged'],
    });

    const listed = await client.send<{ fieldUpdateActions: any[] }>('list_business_rules', { xml: patchedXml });
    const action = listed.fieldUpdateActions.find((a: any) => a.fieldKey === 'F_TestInt');
    expect(action).toBeDefined();
    expect(action.actionId).toBe(2);
    expect(action.parameters).toContain('F_TestDecimal');
  });
});
```

- [ ] **Step 2: 验证失败**

Run: `pnpm test tests/erp/bridge/`，expect FAIL — `add_field_update_action` op 不存在。

- [ ] **Step 3: bridge 实现 — 类似 AddEntityServiceRule 但目标是 Field.UpdateActions**

```csharp
public string AddFieldUpdateAction(string xml, AddFieldUpdateActionArgs args)
{
    if (string.IsNullOrEmpty(args.FieldKey)) throw new ArgumentException("fieldKey is empty");

    var formMeta = _serializer.DeserializeFromString(xml)
        ?? throw new InvalidOperationException("DeserializeFromString returned null");

    var field = FindField(formMeta, args.FieldKey)
        ?? throw new InvalidOperationException($"field '{args.FieldKey}' not found in form metadata");

    var fieldType = field.GetType();
    var actionsProp = fieldType.GetProperty("UpdateActions")
        ?? throw new InvalidOperationException($"field '{args.FieldKey}' has no UpdateActions property");
    var actionsList = actionsProp.GetValue(field) as IList
        ?? (IList)Activator.CreateInstance(actionsProp.PropertyType)!;
    actionsProp.SetValue(field, actionsList);

    foreach (var svc in args.Services)
    {
        var svcType = ResolveServiceMetaType(svc.ClassName);
        var instance = Activator.CreateInstance(svcType)!;
        SetProp(instance, "Id", Guid.NewGuid().ToString("N"));
        if (svc.Parameters != null && svc.Parameters.Length > 0)
        {
            // FormBusinessService.Parameters is typed string (JSON-serialized array of assignments)
            SetProp(instance, "Parameters", JsonConvert.SerializeObject(svc.Parameters));
        }
        foreach (var evt in args.DisabledEvents ?? Array.Empty<string>())
        {
            var prop = svcType.GetProperty(evt);
            if (prop != null && prop.CanWrite) prop.SetValue(instance, "DisableRaise");
        }
        actionsList.Add(instance);
    }

    return _serializer.SerializeToString(formMeta, null);
}
```

Program.cs 加 case `"add_field_update_action"`。

- [ ] **Step 4: Build + run test → PASS**

- [ ] **Step 5: Commit**

```bash
git add bos-bridge/ tests/erp/bridge/integration.test.ts
git commit -m "feat(bridge): add_field_update_action op for Calculate UpdateActions (Plan 5.12.3b)"
```

---

### Task 2.4: bridge op `remove_business_rule`

**Files:**
- Modify: `bos-bridge/BosContext.cs`
- Modify: `bos-bridge/Program.cs`
- Modify: `tests/erp/bridge/integration.test.ts`

- [ ] **Step 1: 写测试 — add 一条 entity rule 拿 patched xml → remove → list 验证消失**

```typescript
describeIfBridge('bos-bridge: remove_business_rule', () => {
  it('removes entity service rule by id, list confirms gone', async () => {
    const inputXml = readFileSync('<fixture-dir>/business-rules-no-rules.xml', 'utf8');
    const ruleId = '22222222-2222-2222-2222-222222222222';

    const { xml: withRule } = await client.send<{ xml: string }>('add_entity_service_rule', {
      xml: inputXml,
      ruleId,
      description: 'temp',
      preCondition: 'True',
      services: [{ className: 'GetInvStockBusinessServiceMeta', actionId: 67, properties: {} }],
    });

    const { xml: removed, location } = await client.send<{ xml: string; location: string }>('remove_business_rule', {
      xml: withRule,
      ruleId,
    });
    expect(location).toBe('entity');

    const listed = await client.send<{ entityRules: any[] }>('list_business_rules', { xml: removed });
    expect(listed.entityRules.find((r: any) => r.id === ruleId)).toBeUndefined();
  });

  it('removes field update action by id, location reports "field"', async () => {
    const inputXml = readFileSync('<fixture-dir>/business-rules-no-rules.xml', 'utf8');

    const { xml: withAction } = await client.send<{ xml: string }>('add_field_update_action', {
      xml: inputXml,
      fieldKey: 'F_TestInt',
      services: [{ className: 'FormBusinessService', actionId: 2, parameters: ['F_TestDecimal = 1'] }],
    });
    const listed1 = await client.send<{ fieldUpdateActions: any[] }>('list_business_rules', { xml: withAction });
    const serviceId = listed1.fieldUpdateActions[0].serviceId;

    const { location } = await client.send<{ xml: string; location: string }>('remove_business_rule', {
      xml: withAction,
      ruleId: serviceId,
    });
    expect(location).toBe('field');
  });

  it('throws clearly when ruleId not found', async () => {
    const inputXml = readFileSync('<fixture-dir>/business-rules-no-rules.xml', 'utf8');
    await expect(
      client.send('remove_business_rule', { xml: inputXml, ruleId: 'does-not-exist' })
    ).rejects.toThrow(/not found/);
  });
});
```

- [ ] **Step 2: 验证失败**

- [ ] **Step 3: bridge 实现 — 扫 entity rules 再扫 field UpdateActions，删第一个 Id 命中**

```csharp
public RemoveBusinessRuleResult RemoveBusinessRule(string xml, string ruleId)
{
    if (string.IsNullOrEmpty(ruleId)) throw new ArgumentException("ruleId is empty");

    var formMeta = _serializer.DeserializeFromString(xml)
        ?? throw new InvalidOperationException("DeserializeFromString returned null");

    foreach (var entity in EnumerateEntities(formMeta))
    {
        var rules = (IList?)entity.GetType().GetProperty("EntityServiceRules")?.GetValue(entity);
        if (rules == null) continue;
        for (int i = 0; i < rules.Count; i++)
        {
            if ((string?)rules[i].GetType().GetProperty("Id")?.GetValue(rules[i]) == ruleId)
            {
                rules.RemoveAt(i);
                return new RemoveBusinessRuleResult
                {
                    Xml = _serializer.SerializeToString(formMeta, null),
                    Location = "entity",
                };
            }
        }
    }

    foreach (var field in EnumerateFields(formMeta))
    {
        var actions = (IList?)field.GetType().GetProperty("UpdateActions")?.GetValue(field);
        if (actions == null) continue;
        for (int i = 0; i < actions.Count; i++)
        {
            if ((string?)actions[i].GetType().GetProperty("Id")?.GetValue(actions[i]) == ruleId)
            {
                actions.RemoveAt(i);
                return new RemoveBusinessRuleResult
                {
                    Xml = _serializer.SerializeToString(formMeta, null),
                    Location = "field",
                };
            }
        }
    }

    throw new InvalidOperationException($"rule '{ruleId}' not found");
}
```

Program.cs Dispatch 加 case，结构 `new { xml = result.Xml, location = result.Location }`。

- [ ] **Step 4: Build + run test → PASS**

- [ ] **Step 5: Commit**

```bash
git add bos-bridge/ tests/erp/bridge/integration.test.ts
git commit -m "feat(bridge): remove_business_rule op (Plan 5.12.3b)"
```

---

## Phase 3 — Agent 工具（Task 3.1 ~ 3.5）

### Task 3.1: TS connector wrapper 方法

**Files:**
- Modify: `src/main/erp/k3cloud/connector.ts`

**Architecture:** 与 5.12.4 v2 转换规则不同的是——业务规则不依赖项目本地持久化的 `loadConvertRuleExtState`，而是每次从 K/3 server 拉当前扩展 FKERNELXML（用现有 `connector.getKernelXml(extId)`），patch 后调 `SaveForIDEV9`。**写回路径**参考 `src/main/agent/bos-rpc-tools.ts` 中 `loadExtensionForSave` + 后续 SaveForIDEV9 调用（已落地的 add-fields / create-entry 工具同形）。

> ⚠️ **Implementer 注意**：write-back 路径（如何把 patched FKERNELXML 转成 `SaveExtensionRequest` 然后走 `SaveForIDEV9`）是这个 task 的最大未知 —— 现有 add-fields 工具走的是 typed AST → DCXML 路径，**不是** patched-xml 路径。可能需要新写一个 helper（接受完整 patched FKERNELXML，构造能让 server 接受的 SaveForIDEV9 ap0 payload），或者沿用 5.12.4 v2 转换规则的 wire-transform 思路。**先 spike 这一步**：拉一个真扩展的 FKERNELXML、bridge patch（加 entity rule）、自己组装 SaveForIDEV9 payload 推回去看 server 有没有 reject。如果走不通就 BLOCKED 报回来，我们一起补 wire 实证。

- [ ] **Step 1: spike write-back 路径**

在临时 `.scratch/` 脚本里跑一次：
1. `connector.getKernelXml(realExtFid)` 拉 FKERNELXML
2. `bridge.send('add_entity_service_rule', { xml, ... })` 拿 patched XML
3. 用现有 RPC helper 构造 SaveForIDEV9 payload（参考 `src/main/erp/k3cloud/rpc/save-for-ide.ts` 或 `bos-rpc-tools.ts` 里 add_fields 调用栈）推回
4. K/3 客户端重登 → BOS Designer 打开扩展看规则在不在

如果 spike 跑不通，记录 server 响应错误码，**BLOCKED 上报**，不要硬写。

- [ ] **Step 2: 加 4 个 wrapper 方法**

成功 spike 后，把 spike 路径产品化到 `K3CloudConnector` 类内，方法签名：

```typescript
async listBusinessRules(extensionFid: string): Promise<{
  entityRules: Array<{ id: string; description: string; entityKey: string; preCondition: string; services: Array<{ actionId: number; className: string; id: string }> }>;
  fieldUpdateActions: Array<{ fieldKey: string; serviceId: string; actionId: number; className: string; parameters?: string }>;
}> {
  const xml = await this.getKernelXml(extensionFid);
  if (!xml) throw new Error(`扩展 ${extensionFid} 无 FKERNELXML`);
  const bridge = await getBridge();
  return bridge.send('list_business_rules', { xml });
}

async addEntityServiceRule(args: {
  extensionFid: string; ruleId: string; description: string;
  preCondition: string; preConditionDesc?: string; entityKey?: string;
  services: Array<{ className: string; actionId: number; properties: Record<string, unknown> }>;
}): Promise<{ ruleId: string }> {
  const xml = await this.getKernelXml(args.extensionFid);
  if (!xml) throw new Error(`扩展 ${args.extensionFid} 无 FKERNELXML`);
  const bridge = await getBridge();
  const { xml: patched } = await bridge.send<{ xml: string }>('add_entity_service_rule', { xml, ...args });
  await this.saveBusinessRulePatch(args.extensionFid, patched);  // spike 落地的新 helper
  return { ruleId: args.ruleId };
}

async addFieldUpdateAction(args: {
  extensionFid: string; fieldKey: string;
  services: Array<{ className: string; actionId: number; parameters?: string[] }>;
  disabledEvents?: string[];
}): Promise<{ serviceId: string }> {
  const xml = await this.getKernelXml(args.extensionFid);
  const bridge = await getBridge();
  const { xml: patched } = await bridge.send<{ xml: string }>('add_field_update_action', { xml, ...args });
  await this.saveBusinessRulePatch(args.extensionFid, patched);
  // 调 list 拿新 serviceId（add op 不返回 id，因为 GUID 在 bridge 内生成）
  const list = await this.listBusinessRules(args.extensionFid);
  const last = list.fieldUpdateActions.filter(a => a.fieldKey === args.fieldKey).pop();
  return { serviceId: last?.serviceId ?? '' };
}

async removeBusinessRule(extensionFid: string, ruleId: string): Promise<{ location: 'entity' | 'field' }> {
  const xml = await this.getKernelXml(extensionFid);
  const bridge = await getBridge();
  const { xml: patched, location } = await bridge.send<{ xml: string; location: 'entity' | 'field' }>(
    'remove_business_rule', { xml, ruleId },
  );
  await this.saveBusinessRulePatch(extensionFid, patched);
  return { location };
}

// helper（spike 落地，sig 待 Step 1 后定）
private async saveBusinessRulePatch(extensionFid: string, patchedXml: string): Promise<void> {
  // ... 调 SaveForIDEV9 推 patched FKERNELXML 回去
}
```

- [ ] **Step 3: 跑 connector 单测**

Run: `pnpm test tests/erp/k3cloud-connector.test.ts`
Expected: PASS（无回归。新 wrapper 因依赖 K/3 session，单测可能 mock connector + bridge，或在 integration test 里覆盖）

- [ ] **Step 4: Commit**

```bash
git add src/main/erp/k3cloud/connector.ts
git commit -m "feat(connector): add business-rule wrapper methods (Plan 5.12.3b)"
```

---

### Task 3.2: agent tool `k3cloud_list_business_rules` + `k3cloud_delete_business_rule` + `k3cloud_describe_service_meta`

**Files:**
- Create: `src/main/agent/business-rule-tools.ts`
- Modify: `src/main/agent/k3cloud-tools.ts`
- Create: `tests/agent/business-rule-tools.test.ts`

- [ ] **Step 1: 写工具单测**

Create `tests/agent/business-rule-tools.test.ts`：

```typescript
import { describe, it, expect, vi } from 'vitest';
import { registerBusinessRuleTools } from '../../src/main/agent/business-rule-tools';
import { ToolRegistry } from '../../src/main/agent/tools';

const mockConnector = {
  listBusinessRules: vi.fn(),
  removeBusinessRule: vi.fn(),
};

describe('business-rule-tools', () => {
  it('registers k3cloud_list_business_rules / delete / describe_service_meta', () => {
    const reg = new ToolRegistry();
    registerBusinessRuleTools(reg, mockConnector as any, 'project-id');
    const names = reg.list().map(t => t.name);
    expect(names).toContain('k3cloud_list_business_rules');
    expect(names).toContain('k3cloud_delete_business_rule');
    expect(names).toContain('k3cloud_describe_service_meta');
  });

  it('list_business_rules calls connector and returns aggregated results', async () => {
    mockConnector.listBusinessRules.mockResolvedValue({
      entityRules: [{ id: 'r1', description: 'test', entityKey: 'HeadEntity', preCondition: 'a > 0', services: [{ actionId: 67, className: 'GetInvStockBusinessServiceMeta', id: 's1' }] }],
      fieldUpdateActions: [],
    });
    const reg = new ToolRegistry();
    registerBusinessRuleTools(reg, mockConnector as any, 'project-id');
    const tool = reg.get('k3cloud_list_business_rules')!;
    const result = await tool.run({ extensionFid: 'fid' });
    expect(mockConnector.listBusinessRules).toHaveBeenCalledWith('fid');
    expect(result.entityRules).toHaveLength(1);
  });

  it('describe_service_meta returns schema for known ActionId', async () => {
    const reg = new ToolRegistry();
    registerBusinessRuleTools(reg, mockConnector as any, 'project-id');
    const tool = reg.get('k3cloud_describe_service_meta')!;
    const result = await tool.run({ actionId: 67 });
    expect(result.actionId).toBe(67);
    expect(result.className).toBe('GetInvStockBusinessServiceMeta');
    expect(result.properties).toBeDefined();
    expect(Object.keys(result.properties)).toContain('stockQtyField');
  });

  it('describe_service_meta returns clear error for unsupported ActionId', async () => {
    const reg = new ToolRegistry();
    registerBusinessRuleTools(reg, mockConnector as any, 'project-id');
    const tool = reg.get('k3cloud_describe_service_meta')!;
    const result = await tool.run({ actionId: 42 });
    expect(result.error).toMatch(/v0\.1 仅支持 ActionId 2 \(Calculate\) 和 67 \(GetInvStock\)/);
  });
});
```

- [ ] **Step 2: 验证失败**

Run: `pnpm test tests/agent/business-rule-tools.test.ts`
Expected: FAIL — module not found

- [ ] **Step 3: 实现 business-rule-tools.ts（先做 list / delete / describe）**

Create `src/main/agent/business-rule-tools.ts`：

```typescript
import type { ToolRegistry, Tool } from './tools';
import type { K3CloudConnector } from '../erp/k3cloud/connector';

const SERVICE_META_SCHEMAS = {
  2: {
    className: 'FormBusinessService',
    description: 'Calculate — IronPython 赋值表达式',
    properties: {
      parameters: { type: 'string[]', description: 'IronPython 赋值数组，如 [" F金额 = F数量 * F单价 "]' },
    },
  },
  67: {
    className: 'GetInvStockBusinessServiceMeta',
    description: 'GetInvStock — 查可用库存',
    properties: {
      stockQtyField:    { type: 'string', default: 'FInvQty',          description: '库存数量目标字段' },
      awaitQtyField:    { type: 'string', default: 'FAwaitQty',        description: '在途数量目标字段' },
      availableQtyField:{ type: 'string', default: 'FAvbQty',          description: '可用数量目标字段' },
      deliQtyFrom:      { type: 'string', default: 'SAL_DELIVERYNOTICE', description: '在途来源单据' },
      deliQtyBillStatus:{ type: 'string', default: 'SAVE',             description: '在途单据状态' },
      stockOrgField:    { type: 'string', default: 'FSTOCKORGID',      description: '库存组织字段' },
      keeperTypeField:  { type: 'string', default: 'FKEEPERTYPEID',    description: '保管者类型字段' },
      keeperField:      { type: 'string', default: 'FKEEPERID',        description: '保管者字段' },
      ownerTypeField:   { type: 'string', default: 'FOWNERTYPEID',     description: '货主类型字段' },
      ownerField:       { type: 'string', default: 'FOWNERID',         description: '货主字段' },
      stockField:       { type: 'string', default: 'FSTOCKID',         description: '仓库字段' },
      stockPlaceField:  { type: 'string', default: 'FSTOCKLOCID',      description: '仓位字段' },
      materialField:    { type: 'string', default: 'FMATERIALID',      description: '物料字段' },
      // wire 实证补充字段
      extAuxQtyField:    { type: 'string', description: '辅助数量字段' },
      returnQtyField:    { type: 'integer', description: '是否返回数量（1=返回）' },
      pluginClassName:   { type: 'string', description: '自定义插件类名' },
      stockStatusField:  { type: 'string', description: '库存状态字段' },
      projectNoField:    { type: 'string', description: '项目编号字段' },
      secUnitIdField:    { type: 'string', description: '辅助单位字段' },
      extAuxUnitIdField: { type: 'string', description: '辅助单位 ID 字段' },
    },
  },
} as const;

export function registerBusinessRuleTools(
  reg: ToolRegistry,
  connector: K3CloudConnector,
  projectId: string,
): Tool[] {
  const tools: Tool[] = [
    {
      name: 'k3cloud_list_business_rules',
      description: '列出指定 BOS 扩展上的所有业务规则 (实体级 EntityServiceRule + 字段级 UpdateActions)。',
      parallelSafe: true,
      inputSchema: {
        type: 'object',
        properties: {
          extensionFid: { type: 'string', description: '扩展对象的 FID' },
        },
        required: ['extensionFid'],
      },
      run: async ({ extensionFid }: { extensionFid: string }) => {
        return await connector.listBusinessRules(extensionFid);
      },
    },
    {
      name: 'k3cloud_delete_business_rule',
      description: '删除指定 ruleId 的业务规则 (自动判断是 EntityServiceRule 还是字段 UpdateAction)。',
      inputSchema: {
        type: 'object',
        properties: {
          extensionFid: { type: 'string' },
          ruleId: { type: 'string', description: '规则的 GUID' },
        },
        required: ['extensionFid', 'ruleId'],
      },
      run: async ({ extensionFid, ruleId }: { extensionFid: string; ruleId: string }) => {
        return await connector.removeBusinessRule(extensionFid, ruleId);
      },
    },
    {
      name: 'k3cloud_describe_service_meta',
      description: '查询某 ActionId 对应的 service 类参数 schema (字段映射默认值 / 类型 / 描述)。v0.1 支持 2 (Calculate) 和 67 (GetInvStock)。',
      parallelSafe: true,
      inputSchema: {
        type: 'object',
        properties: { actionId: { type: 'integer' } },
        required: ['actionId'],
      },
      run: async ({ actionId }: { actionId: number }) => {
        const schema = SERVICE_META_SCHEMAS[actionId as keyof typeof SERVICE_META_SCHEMAS];
        if (!schema) {
          return { error: `v0.1 仅支持 ActionId 2 (Calculate) 和 67 (GetInvStock)。其他 ActionId (3/23/42/70 等) 留 v0.2，可用 k3cloud_register_plugin 写 Python 插件作为替代。` };
        }
        return { actionId, ...schema };
      },
    },
  ];

  for (const t of tools) reg.register(t);
  return tools;
}
```

- [ ] **Step 4: 注册到 k3cloud-tools.ts**

Edit `src/main/agent/k3cloud-tools.ts` 在 register 函数末尾加：

```typescript
import { registerBusinessRuleTools } from './business-rule-tools';
// ... in registration function:
registerBusinessRuleTools(reg, connector, projectId);
```

- [ ] **Step 5: 跑测试**

Run: `pnpm test tests/agent/business-rule-tools.test.ts tests/agent/k3cloud-tools.test.ts`
Expected: PASS（含 `k3cloud_` prefix 测试，新增 3 个工具自动满足）

- [ ] **Step 6: Commit**

```bash
git add src/main/agent/business-rule-tools.ts src/main/agent/k3cloud-tools.ts tests/agent/business-rule-tools.test.ts
git commit -m "feat(tools): k3cloud_list_business_rules + delete + describe_service_meta (Plan 5.12.3b)"
```

---

### Task 3.3: agent tool `k3cloud_add_get_inv_stock_rule`

**Files:**
- Modify: `src/main/agent/business-rule-tools.ts`
- Modify: `tests/agent/business-rule-tools.test.ts`
- Create: `src/main/agent/validators/field-existence.ts`
- Create: `tests/agent/validators/field-existence.test.ts`

- [ ] **Step 1: 写 field-existence validator 单测**

Create `tests/agent/validators/field-existence.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import { validateFieldExistence } from '../../../src/main/agent/validators/field-existence';

const SCHEMA = {
  fields: ['FBillNo', 'FCustId', 'FAmount', 'FQty', 'FPrice'],
};

describe('field-existence validator', () => {
  it('passes when all referenced fields exist', () => {
    const result = validateFieldExistence(['FAmount', 'FQty', 'FPrice'], SCHEMA);
    expect(result.ok).toBe(true);
  });

  it('rejects unknown field with suggestions', () => {
    const result = validateFieldExistence(['FCustomerId'], SCHEMA);
    expect(result.ok).toBe(false);
    expect(result.errors).toHaveLength(1);
    expect(result.errors![0].field).toBe('FCustomerId');
    expect(result.errors![0].suggestions).toEqual(expect.arrayContaining(['FCustId']));
  });

  it('lists multiple missing fields with separate errors', () => {
    const result = validateFieldExistence(['FFoo', 'FBar', 'FAmount'], SCHEMA);
    expect(result.ok).toBe(false);
    expect(result.errors).toHaveLength(2);
  });
});
```

- [ ] **Step 2: 验证失败**

Run: `pnpm test tests/agent/validators/field-existence.test.ts`
Expected: FAIL — module not found

- [ ] **Step 3: 实现 validator**

Create `src/main/agent/validators/field-existence.ts`：

```typescript
export interface FieldSchema {
  fields: string[];  // 父对象 + 所有 entity 的 field key 集合
}

export interface ValidationError {
  field: string;
  suggestions: string[];
}

export interface ValidationResult {
  ok: boolean;
  errors?: ValidationError[];
}

export function validateFieldExistence(referencedFields: string[], schema: FieldSchema): ValidationResult {
  const set = new Set(schema.fields);
  const errors: ValidationError[] = [];
  for (const field of referencedFields) {
    if (!set.has(field)) {
      errors.push({
        field,
        suggestions: nearestMatches(field, schema.fields, 3),
      });
    }
  }
  return errors.length > 0 ? { ok: false, errors } : { ok: true };
}

function nearestMatches(target: string, candidates: string[], n: number): string[] {
  const scored = candidates.map(c => ({ c, d: levenshtein(target, c) }));
  scored.sort((a, b) => a.d - b.d);
  return scored.slice(0, n).map(s => s.c);
}

function levenshtein(a: string, b: string): number {
  const m = a.length, n = b.length;
  if (m === 0) return n;
  if (n === 0) return m;
  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));
  for (let i = 0; i <= m; i++) dp[i][0] = i;
  for (let j = 0; j <= n; j++) dp[0][j] = j;
  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      if (a[i - 1] === b[j - 1]) dp[i][j] = dp[i - 1][j - 1];
      else dp[i][j] = 1 + Math.min(dp[i - 1][j - 1], dp[i - 1][j], dp[i][j - 1]);
    }
  }
  return dp[m][n];
}
```

- [ ] **Step 4: 跑 validator 测试 → PASS**

- [ ] **Step 5: 写 add_get_inv_stock_rule 工具测试**

Add to `tests/agent/business-rule-tools.test.ts`:

```typescript
it('add_get_inv_stock_rule: happy path', async () => {
  mockConnector.getExtensionFields = vi.fn().mockResolvedValue({
    fields: ['F_TestQty', 'F_TestStock', 'F_BillNo', 'FBillTypeID'],
  });
  mockConnector.addEntityServiceRule = vi.fn().mockResolvedValue({ ok: true, ruleId: 'r1' });

  const reg = new ToolRegistry();
  registerBusinessRuleTools(reg, mockConnector as any, 'project-id');
  const tool = reg.get('k3cloud_add_get_inv_stock_rule')!;
  const result = await tool.run({
    extensionFid: 'fid',
    description: 'test',
    preCondition: " FBillTypeID.FNumber = '01.01'",
    stockQtyField: 'F_TestQty',
  });
  expect(result.ok).toBe(true);
  expect(mockConnector.addEntityServiceRule).toHaveBeenCalledWith(
    expect.objectContaining({
      services: expect.arrayContaining([
        expect.objectContaining({ className: 'GetInvStockBusinessServiceMeta', actionId: 67 }),
      ]),
    }),
  );
});

it('add_get_inv_stock_rule: rejects unknown field', async () => {
  mockConnector.getExtensionFields = vi.fn().mockResolvedValue({
    fields: ['F_TestQty', 'FBillTypeID'],
  });
  const reg = new ToolRegistry();
  registerBusinessRuleTools(reg, mockConnector as any, 'project-id');
  const tool = reg.get('k3cloud_add_get_inv_stock_rule')!;
  const result = await tool.run({
    extensionFid: 'fid',
    description: 'test',
    preCondition: 'FBillTypeID is not null',
    stockQtyField: 'F_NonExistent',
  });
  expect(result.error).toMatch(/F_NonExistent/);
  expect(result.error).toMatch(/F_TestQty/);  // suggestion
});

it('add_get_inv_stock_rule: rejects empty preCondition', async () => {
  const reg = new ToolRegistry();
  registerBusinessRuleTools(reg, mockConnector as any, 'project-id');
  const tool = reg.get('k3cloud_add_get_inv_stock_rule')!;
  const result = await tool.run({
    extensionFid: 'fid',
    description: 'test',
    preCondition: '',  // empty
    stockQtyField: 'F_TestQty',
  });
  expect(result.error).toMatch(/preCondition.*必填/);
});
```

- [ ] **Step 6: 实现 add_get_inv_stock_rule 工具**

Edit `src/main/agent/business-rule-tools.ts`，在 tools 数组里加：

```typescript
{
  name: 'k3cloud_add_get_inv_stock_rule',
  description: '在 BOS 扩展的实体级加一条 GetInvStock (查可用库存) 业务规则。preCondition 必填非空。',
  inputSchema: {
    type: 'object',
    properties: {
      extensionFid: { type: 'string' },
      description: { type: 'string', description: '规则中文名' },
      preCondition: { type: 'string', description: 'IronPython 布尔表达式，必填非空' },
      preConditionDesc: { type: 'string', description: '前置条件中文描述' },
      // 字段映射 ~19 个 (参考 docs/recon §4.1 / k3cloud_describe_service_meta)
      stockQtyField:    { type: 'string', default: 'FInvQty' },
      awaitQtyField:    { type: 'string', default: 'FAwaitQty' },
      // ... 其余字段全列出（按 SERVICE_META_SCHEMAS[67].properties）
    },
    required: ['extensionFid', 'description', 'preCondition'],
  },
  run: async (args: any) => {
    if (!args.preCondition || args.preCondition.trim() === '') {
      return { error: 'preCondition 必填非空 (BOS Designer 强制要求实体服务规则有前置条件)' };
    }
    // Validate referenced fields exist
    const schema = await connector.getExtensionFields(args.extensionFid);
    const referenced: string[] = [];
    for (const k of Object.keys(SERVICE_META_SCHEMAS[67].properties)) {
      if (args[k] && typeof args[k] === 'string') referenced.push(args[k]);
    }
    const validation = validateFieldExistence(referenced, schema);
    if (!validation.ok) {
      return {
        error: validation.errors!.map(e =>
          `字段 ${e.field} 不存在。可选: ${e.suggestions.join(' / ')}`
        ).join('\n'),
      };
    }
    // Build properties from non-default args
    const properties: Record<string, unknown> = {};
    const defaults = SERVICE_META_SCHEMAS[67].properties;
    for (const [k, v] of Object.entries(args)) {
      if (k in defaults && v !== undefined) properties[k] = v;
    }
    return await connector.addEntityServiceRule({
      extensionFid: args.extensionFid,
      ruleId: crypto.randomUUID(),
      description: args.description,
      preCondition: args.preCondition,
      preConditionDesc: args.preConditionDesc,
      services: [{ className: 'GetInvStockBusinessServiceMeta', actionId: 67, properties }],
    });
  },
},
```

- [ ] **Step 7: 跑测试 → PASS**

Run: `pnpm test tests/agent/business-rule-tools.test.ts tests/agent/validators/`
Expected: 全 PASS

- [ ] **Step 8: Commit**

```bash
git add src/main/agent/validators/ src/main/agent/business-rule-tools.ts tests/agent/
git commit -m "feat(tools): k3cloud_add_get_inv_stock_rule + field-existence validator (Plan 5.12.3b)"
```

---

### Task 3.4: IronPython AST validator

**Files:**
- Create: `src/main/agent/validators/ironpython-ast.ts`
- Create: `src/main/agent/validators/calculate-rule.ts`
- Create: `tests/agent/validators/ironpython-ast.test.ts`
- Create: `tests/agent/validators/calculate-rule.test.ts`

**Approach:** v0.1 不引入完整 Python AST 解析器。用**简化的 tokenizer + 模式匹配**——抽出函数调用 / 字段引用 / SQL-style 禁止模式 / 赋值结构。

- [ ] **Step 1: 写 ironpython-ast 测试**

Create `tests/agent/validators/ironpython-ast.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import { extractCallsAndFields, parseAssignment } from '../../../src/main/agent/validators/ironpython-ast';

describe('ironpython-ast extraction', () => {
  it('extracts function calls', () => {
    const result = extractCallsAndFields('round(F金额 * 1.13, 2)');
    expect(result.functions).toEqual(['round']);
    expect(result.fields).toEqual(['F金额']);
  });

  it('extracts dotted field access', () => {
    const result = extractCallsAndFields('FCustId.FNumber == "VIP"');
    expect(result.fields).toEqual(['FCustId', 'FCustId.FNumber']);
  });

  it('extracts GetFieldValue calls', () => {
    const result = extractCallsAndFields('GetFieldValue("FQty") * GetFieldValue("FPrice")');
    expect(result.functions).toEqual(['GetFieldValue']);
    expect(result.fieldStringRefs).toEqual(['FQty', 'FPrice']);
  });

  it('parses simple assignment', () => {
    const result = parseAssignment('F金额 = F数量 * F单价');
    expect(result.ok).toBe(true);
    expect(result.target).toBe('F金额');
    expect(result.expression).toBe('F数量 * F单价');
  });

  it('rejects bare expression (no =)', () => {
    const result = parseAssignment('F数量 * F单价');
    expect(result.ok).toBe(false);
    expect(result.error).toMatch(/必须是赋值/);
  });

  it('handles whitespace-padded assignments', () => {
    const result = parseAssignment('  F金额  =  F数量 * F单价  ');
    expect(result.ok).toBe(true);
    expect(result.target).toBe('F金额');
  });
});
```

- [ ] **Step 2: 验证失败**

- [ ] **Step 3: 实现 ironpython-ast.ts**

Create `src/main/agent/validators/ironpython-ast.ts`：

```typescript
const FIELD_PATTERN = /\bF[\p{Script=Han}A-Za-z0-9_]+(?:\.F?[A-Za-z][A-Za-z0-9_]*)?/gu;
const FUNCTION_PATTERN = /\b([A-Z][A-Za-z0-9_]+)\s*\(/g;
const GET_FIELD_VALUE_PATTERN = /GetFieldValue\(\s*["']([^"']+)["']\s*\)/g;
const ASSIGNMENT_PATTERN = /^\s*(F[\p{Script=Han}A-Za-z0-9_]+)\s*=\s*(.+?)\s*$/u;

export interface ExtractionResult {
  functions: string[];
  fields: string[];
  fieldStringRefs: string[];
}

export function extractCallsAndFields(source: string): ExtractionResult {
  const functions = new Set<string>();
  const fields = new Set<string>();
  const fieldStringRefs = new Set<string>();

  for (const m of source.matchAll(FUNCTION_PATTERN)) functions.add(m[1]);
  for (const m of source.matchAll(FIELD_PATTERN)) fields.add(m[0]);
  for (const m of source.matchAll(GET_FIELD_VALUE_PATTERN)) fieldStringRefs.add(m[1]);

  return {
    functions: [...functions],
    fields: [...fields],
    fieldStringRefs: [...fieldStringRefs],
  };
}

export interface ParseAssignmentResult {
  ok: boolean;
  target?: string;
  expression?: string;
  error?: string;
}

export function parseAssignment(source: string): ParseAssignmentResult {
  const m = source.match(ASSIGNMENT_PATTERN);
  if (!m) {
    return { ok: false, error: '必须是赋值语句 <Field> = <Expression>，不能是裸表达式' };
  }
  return { ok: true, target: m[1], expression: m[2] };
}
```

- [ ] **Step 4: 跑测试 → PASS**

- [ ] **Step 5: 写 calculate-rule validator 测试**

Create `tests/agent/validators/calculate-rule.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import { validateCalculateRule } from '../../../src/main/agent/validators/calculate-rule';

const SCHEMA = { fields: ['F金额', 'F数量', 'F单价', 'F税额', 'FCustId', 'FBillTypeID'] };

describe('calculate-rule validator', () => {
  it('passes valid IronPython assignment', () => {
    const result = validateCalculateRule(['F金额 = F数量 * F单价'], SCHEMA);
    expect(result.ok).toBe(true);
  });

  it('rejects SQL-style ROUND', () => {
    const result = validateCalculateRule(['F金额 = ROUND(F数量 * F单价, 2)'], SCHEMA);
    expect(result.ok).toBe(false);
    expect(result.errors![0].message).toMatch(/ROUND.*SQL.*Python.*round/);
  });

  it('rejects ISNULL', () => {
    const result = validateCalculateRule(['F金额 = ISNULL(F数量, 0)'], SCHEMA);
    expect(result.ok).toBe(false);
    expect(result.errors![0].message).toMatch(/ISNULL/);
  });

  it('rejects unknown field with suggestion', () => {
    const result = validateCalculateRule(['F金额 = F数量 * F單价'], SCHEMA);  // 错字"單"
    expect(result.ok).toBe(false);
    expect(result.errors![0].field).toBe('F單价');
    expect(result.errors![0].suggestions).toContain('F单价');
  });

  it('rejects unknown function with FuncDefine suggestion', () => {
    const result = validateCalculateRule(['F金额 = GetCurrentTime()'], SCHEMA);
    expect(result.ok).toBe(false);
    expect(result.errors![0].message).toMatch(/GetCurrentTime.*GetTime.*GetDate/);
  });

  it('rejects bare expression without =', () => {
    const result = validateCalculateRule(['F数量 * F单价'], SCHEMA);
    expect(result.ok).toBe(false);
    expect(result.errors![0].message).toMatch(/必须是赋值/);
  });

  it('rejects Python 3 print() function', () => {
    const result = validateCalculateRule(['F金额 = print(F数量)'], SCHEMA);  // print() in Python 3 syntax
    expect(result.ok).toBe(false);
    expect(result.errors![0].message).toMatch(/print/);
  });

  it('accepts valid GetFieldValue', () => {
    const result = validateCalculateRule(['F金额 = GetFieldValue("F数量") * GetFieldValue("F单价")'], SCHEMA);
    expect(result.ok).toBe(true);
  });

  it('rejects GetFieldValue with unknown field key', () => {
    const result = validateCalculateRule(['F金额 = GetFieldValue("F幻觉字段")'], SCHEMA);
    expect(result.ok).toBe(false);
    expect(result.errors![0].field).toBe('F幻觉字段');
  });

  it('reports line number for multi-action arrays', () => {
    const result = validateCalculateRule([
      'F金额 = F数量 * F单价',
      'F税额 = ROUND(F金额 * 0.13, 2)',
    ], SCHEMA);
    expect(result.ok).toBe(false);
    expect(result.errors![0].line).toBe(2);
  });
});
```

- [ ] **Step 6: 实现 calculate-rule.ts**

Create `src/main/agent/validators/calculate-rule.ts`：

```typescript
import { extractCallsAndFields, parseAssignment } from './ironpython-ast';
import type { FieldSchema } from './field-existence';

const FUNC_DEFINE_WHITELIST = new Set([
  'GetFlexDetailValue', 'GetPKValue', 'GetAcronym', 'BillTypeParam',
  'IsFloatUnitConvert', 'OperationStatus', 'SysParam',
  'Avg', 'Count', 'IsDraw', 'IsPush',
  'GetCurrOrg', 'GetUser', 'GetFieldValue', 'GetDate', 'GetTime',
]);

const PYTHON_BUILTINS = new Set([
  'len', 'str', 'int', 'float', 'bool', 'round', 'abs', 'max', 'min',
  'sum', 'sorted', 'isinstance', 'type', 'range', 'list', 'dict', 'set',
  'map', 'filter', 'reduce', 'tuple', 'frozenset',
]);

const SQL_STYLE_FUNCS: Record<string, string> = {
  IIF: '用 Python 三目: 值A if 条件 else 值B',
  CONCAT: '用 + 拼接字符串',
  DATEADD: '用 .NET: F日期.AddDays(N)',
  ISNULL: '用 Python: x if x is not None else 默认值',
  DATEDIFF: '用 .NET: (F日期1 - F日期2).Days',
  LEN: '用 Python 全小写 len(x)',
  ROUND: '用 Python 全小写 round(x, n)',
  SUBSTR: '用 Python 切片 x[a:b]',
  UPPER: '用 Python: x.upper()',
  LOWER: '用 Python: x.lower()',
};

const PYTHON_3_PATTERNS = [
  { pattern: /\bprint\s*\(/, message: 'print() 是 Python 3 语法。IronPython 2.7 用 print 语句不带括号' },
  { pattern: /\bf["']/, message: 'f-string 是 Python 3.6+ 语法。IronPython 2.7 不支持，用 .format() 或 % 拼接' },
  { pattern: /\basync\s+def\b/, message: 'async/await 是 Python 3.5+ 语法。IronPython 2.7 不支持' },
];

const FUNC_TYPO_HINTS: Record<string, string[]> = {
  GetCurrentTime: ['GetTime', 'GetDate'],
  Now: ['GetDate', 'GetTime'],
  Today: ['GetDate'],
  CurrentUser: ['GetUser'],
};

export interface CalcValidationError {
  line: number;
  field?: string;
  suggestions?: string[];
  message: string;
}

export interface CalcValidationResult {
  ok: boolean;
  errors?: CalcValidationError[];
}

export function validateCalculateRule(actions: string[], schema: FieldSchema): CalcValidationResult {
  const errors: CalcValidationError[] = [];
  const knownFields = new Set(schema.fields);

  actions.forEach((action, i) => {
    const line = i + 1;
    const stripped = action.trim();

    // 1. Parse assignment structure
    const parsed = parseAssignment(stripped);
    if (!parsed.ok) {
      errors.push({ line, message: parsed.error! });
      return;
    }

    // 2. Check Python 3 patterns
    for (const { pattern, message } of PYTHON_3_PATTERNS) {
      if (pattern.test(stripped)) {
        errors.push({ line, message });
      }
    }

    // 3. Extract functions and fields
    const { functions, fields, fieldStringRefs } = extractCallsAndFields(stripped);

    // 4. Function whitelist check
    for (const fn of functions) {
      if (FUNC_DEFINE_WHITELIST.has(fn)) continue;
      if (PYTHON_BUILTINS.has(fn)) continue;
      if (SQL_STYLE_FUNCS[fn.toUpperCase()]) {
        errors.push({
          line,
          message: `${fn} 是 SQL 风格函数，IronPython 不支持。${SQL_STYLE_FUNCS[fn.toUpperCase()]}`,
        });
        continue;
      }
      const suggestions = FUNC_TYPO_HINTS[fn] || nearestFuncs(fn);
      errors.push({
        line,
        message: `函数 ${fn} 不在白名单。可选: ${suggestions.join(' / ')}`,
      });
    }

    // 5. Field existence (target + referenced fields + GetFieldValue arg fields)
    const allReferenced = new Set<string>();
    if (parsed.target) allReferenced.add(parsed.target);
    for (const f of fields) {
      const head = f.split('.')[0];
      allReferenced.add(head);
    }
    for (const f of fieldStringRefs) allReferenced.add(f);

    for (const f of allReferenced) {
      if (!knownFields.has(f)) {
        errors.push({
          line,
          field: f,
          suggestions: nearestStrings(f, schema.fields),
          message: `字段 ${f} 不在父对象 schema 里`,
        });
      }
    }
  });

  return errors.length > 0 ? { ok: false, errors } : { ok: true };
}

function nearestFuncs(target: string): string[] {
  const all = [...FUNC_DEFINE_WHITELIST, ...PYTHON_BUILTINS];
  return nearestStrings(target, all).slice(0, 3);
}

function nearestStrings(target: string, candidates: string[]): string[] {
  return candidates
    .map(c => ({ c, d: levenshtein(target, c) }))
    .sort((a, b) => a.d - b.d)
    .slice(0, 3)
    .map(s => s.c);
}

function levenshtein(a: string, b: string): number {
  const m = a.length, n = b.length;
  if (m === 0) return n; if (n === 0) return m;
  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));
  for (let i = 0; i <= m; i++) dp[i][0] = i;
  for (let j = 0; j <= n; j++) dp[0][j] = j;
  for (let i = 1; i <= m; i++)
    for (let j = 1; j <= n; j++)
      dp[i][j] = a[i-1] === b[j-1] ? dp[i-1][j-1] : 1 + Math.min(dp[i-1][j-1], dp[i-1][j], dp[i][j-1]);
  return dp[m][n];
}
```

- [ ] **Step 7: 跑测试 → PASS**

Run: `pnpm test tests/agent/validators/`
Expected: 全 PASS

- [ ] **Step 8: Commit**

```bash
git add src/main/agent/validators/ tests/agent/validators/
git commit -m "feat(validators): IronPython AST + Calculate rule validator (Plan 5.12.3b)"
```

---

### Task 3.5: agent tool `k3cloud_add_calculate_rule` (含 retry loop)

**Files:**
- Modify: `src/main/agent/business-rule-tools.ts`
- Modify: `tests/agent/business-rule-tools.test.ts`

- [ ] **Step 1: 写工具测试（含 happy path + 校验失败 + retry 行为）**

Add to `tests/agent/business-rule-tools.test.ts`:

```typescript
it('add_calculate_rule (field-level): happy path', async () => {
  mockConnector.getExtensionFields = vi.fn().mockResolvedValue({
    fields: ['F金额', 'F数量', 'F单价'],
  });
  mockConnector.addFieldUpdateAction = vi.fn().mockResolvedValue({ ok: true, serviceId: 's1' });

  const reg = new ToolRegistry();
  registerBusinessRuleTools(reg, mockConnector as any, 'project-id');
  const tool = reg.get('k3cloud_add_calculate_rule')!;
  const result = await tool.run({
    extensionFid: 'fid',
    mountPoint: { kind: 'field', fieldKey: 'F单价' },
    actions: ['F金额 = F数量 * F单价'],
  });
  expect(result.ok).toBe(true);
});

it('add_calculate_rule rejects SQL-style with retry hint', async () => {
  mockConnector.getExtensionFields = vi.fn().mockResolvedValue({
    fields: ['F金额', 'F数量', 'F单价'],
  });

  const reg = new ToolRegistry();
  registerBusinessRuleTools(reg, mockConnector as any, 'project-id');
  const tool = reg.get('k3cloud_add_calculate_rule')!;
  const result = await tool.run({
    extensionFid: 'fid',
    mountPoint: { kind: 'field', fieldKey: 'F单价' },
    actions: ['F金额 = ROUND(F数量 * F单价, 2)'],
  });
  expect(result.error).toMatch(/ROUND.*round/);
  expect(result.retryHint).toBeDefined();
});

it('add_calculate_rule (entity-level): rejects empty preCondition', async () => {
  const reg = new ToolRegistry();
  registerBusinessRuleTools(reg, mockConnector as any, 'project-id');
  const tool = reg.get('k3cloud_add_calculate_rule')!;
  const result = await tool.run({
    extensionFid: 'fid',
    mountPoint: { kind: 'entity', entityKey: 'HeadEntity', preCondition: '', description: 'test' },
    actions: ['F金额 = F数量 * F单价'],
  });
  expect(result.error).toMatch(/preCondition.*必填/);
});
```

- [ ] **Step 2: 实现 add_calculate_rule 工具**

Edit `src/main/agent/business-rule-tools.ts`，加：

```typescript
import { validateCalculateRule } from './validators/calculate-rule';
// ...
{
  name: 'k3cloud_add_calculate_rule',
  description: 'Calculate (ActionId=2) — IronPython 赋值表达式。支持字段级 UpdateAction 或实体级 EntityServiceRule (实体级 preCondition 必填)。',
  inputSchema: {
    type: 'object',
    properties: {
      extensionFid: { type: 'string' },
      mountPoint: {
        oneOf: [
          {
            type: 'object',
            properties: {
              kind: { const: 'field' },
              fieldKey: { type: 'string' },
              disabledEvents: { type: 'array', items: { type: 'string' } },
            },
            required: ['kind', 'fieldKey'],
          },
          {
            type: 'object',
            properties: {
              kind: { const: 'entity' },
              entityKey: { type: 'string', default: 'HeadEntity' },
              preCondition: { type: 'string', minLength: 1 },
              preConditionDesc: { type: 'string' },
              description: { type: 'string' },
            },
            required: ['kind', 'preCondition', 'description'],
          },
        ],
      },
      actions: {
        type: 'array',
        items: { type: 'string', description: 'IronPython 赋值，如 "F金额 = F数量 * F单价"' },
        minItems: 1,
      },
    },
    required: ['extensionFid', 'mountPoint', 'actions'],
  },
  run: async (args: any) => {
    // Validate entity-level preCondition non-empty
    if (args.mountPoint.kind === 'entity' && !args.mountPoint.preCondition?.trim()) {
      return { error: 'preCondition 必填非空 (BOS Designer 强制要求实体服务规则有前置条件)' };
    }

    // Run AST validator
    const schema = await connector.getExtensionFields(args.extensionFid);
    const validation = validateCalculateRule(args.actions, schema);
    if (!validation.ok) {
      const errMsg = validation.errors!
        .map(e => `[第 ${e.line} 条] ${e.message}${e.suggestions ? ` (提示: ${e.suggestions.join(', ')})` : ''}`)
        .join('\n');
      return {
        error: errMsg,
        retryHint: '修正以上错误后重试。可调 k3cloud_describe_service_meta(actionId=2) 查 IronPython 规则。',
      };
    }

    // Dispatch to bridge
    if (args.mountPoint.kind === 'field') {
      return await connector.addFieldUpdateAction({
        extensionFid: args.extensionFid,
        fieldKey: args.mountPoint.fieldKey,
        services: [{
          className: 'FormBusinessService',
          actionId: 2,
          parameters: args.actions,
        }],
        disabledEvents: args.mountPoint.disabledEvents,
      });
    } else {
      return await connector.addEntityServiceRule({
        extensionFid: args.extensionFid,
        ruleId: crypto.randomUUID(),
        description: args.mountPoint.description,
        preCondition: args.mountPoint.preCondition,
        preConditionDesc: args.mountPoint.preConditionDesc,
        services: [{
          className: 'FormBusinessService',
          actionId: 2,
          properties: { Parameters: JSON.stringify(args.actions) },
        }],
      });
    }
  },
},
```

注：retry loop 实现选择——不在工具层做 retry（让 LLM 看到错误后自己修），返回详细错误 + retryHint 即可。这是 route C 的"validate-and-retry"，retry 由 agent loop 层自然驱动（LLM 收到 error → 修正参数 → 再次调用）。

- [ ] **Step 3: 跑测试 → PASS**

- [ ] **Step 4: Commit**

```bash
git add src/main/agent/business-rule-tools.ts tests/agent/business-rule-tools.test.ts
git commit -m "feat(tools): k3cloud_add_calculate_rule with route C validator (Plan 5.12.3b)"
```

---

## Phase 4 — System Prompt + 文档更新（Task 4.1）

### Task 4.1: 更新 erp-rules/k3cloud.md 加业务规则使用纪律

**Files:**
- Modify: `src/main/agent/prompts/erp-rules/k3cloud.md`

- [ ] **Step 1: Read 现有 prompt**

Run: `cat src/main/agent/prompts/erp-rules/k3cloud.md | head -50`

熟悉 prompt 现有结构再加内容。

- [ ] **Step 2: 加业务规则段（在工具列表后、闭环规则前）**

Edit 加：

```markdown
## 业务规则（v0.1: ActionId=2 Calculate + ActionId=67 GetInvStock）

写业务规则前必经流程：

1. **读取已有**：先 `k3cloud_list_business_rules(extensionFid)` 看现有规则，避免重复
2. **查 schema**：用 `k3cloud_describe_service_meta(actionId=2 或 67)` 拿参数 schema 和默认值
3. **加规则**：
   - Calculate 用 `k3cloud_add_calculate_rule`（IronPython 赋值表达式数组）
   - GetInvStock 用 `k3cloud_add_get_inv_stock_rule`（字段映射对象）
4. **校验失败 retry**：工具返回 error + retryHint 时，**修正后再次调用**（不要换工具或绕开）

### 实体级规则的 preCondition 必填

任何实体级 EntityServiceRule（`mountPoint.kind = 'entity'`）的 `preCondition` 必须非空——BOS Designer 强制要求。

合法示例：
- `OperationStatus() == 'Add'` — 仅新增时触发
- `FBillTypeID.FNumber == '01.01'` — 单据类型为 01.01 时触发
- `FAmt > 1000000` — 金额大于 100 万触发

不允许空字符串 / 空格字符串作为永真条件。

### IronPython 写法纪律（Calculate）

- 函数全小写（`round` 不是 `ROUND`，`len` 不是 `LEN`）
- 字段引用直接用 key（`F金额` 不是 `[F金额]`）
- 三目用 `值A if 条件 else 值B`（不是 `IIF`）
- 字符串拼接用 `+`（不是 `CONCAT`）
- 日期运算用 .NET 方法（`F日期.AddDays(7)` 不是 `DATEADD`）
- 空检查用 `is None` / `is not None`（不是 `ISNULL`）

### v0.1 不支持的 ActionId

3 (MulUnitConvert) / 23 (CallBillFunction) / 42 (GetPrice) / 70 (InvMinusCheck) — 留 v0.2。
客户场景不能用 Calculate 或 GetInvStock 表达时，建议改用 `k3cloud_register_plugin` 写 Python 表单插件实现。
```

- [ ] **Step 3: 跑 prompt 相关测试**

Run: `pnpm test tests/agent/erp-rules.test.ts 2>/dev/null || pnpm test tests/agent/loop.test.ts`

- [ ] **Step 4: Commit**

```bash
git add src/main/agent/prompts/erp-rules/k3cloud.md
git commit -m "docs(prompts): add business-rule usage rules to k3cloud.md (Plan 5.12.3b)"
```

---

## Phase 5 — E2E demo + 文档（Task 5.1）

### Task 5.1: 真账套 e2e 验证

**Files:**
- Create: `docs/demos/2026-05-XX-business-rules-demo.md`

- [ ] **Step 1: 在 dev K/3 Cloud 起一个新对话场景**

OpenDeploy app dev 模式跑：`pnpm dev`

让 agent 完成以下任务（人工对话指令）：

```
【场景 1】在 SAL_SaleOrder 的 dev 扩展上加一条计算规则：单价变化时重算金额（F_TestAmount = F_TestQty * F_TestPrice）。

【场景 2】列出该扩展所有业务规则，确认场景 1 的规则在列。

【场景 3】在该扩展上加一条 GetInvStock 实体级规则，preCondition 是"FBillTypeID.FNumber == '01.01'"，stockQtyField 填 F_TestStock。

【场景 4】用 BOS Designer 打开该扩展，确认两条规则都正确显示。

【场景 5】让 agent 删除场景 3 的 GetInvStock 规则。

【场景 6】列出规则，确认场景 3 的规则消失，场景 1 的规则保留。
```

- [ ] **Step 2: 记录每个场景的 agent 行为 + 用户验证截图**

写到 `docs/demos/2026-05-XX-business-rules-demo.md`：

```markdown
# Plan 5.12.3b — 业务规则 e2e demo

> 实证日期：2026-05-XX
> 测试扩展：SAL_SaleOrder dev 扩展 (FID `<...>`)
> Dev 环境：localhost K/3 Cloud V9.0.553.12

## 场景 1 — Calculate 字段级 UpdateAction

**用户指令**: "[原文]"
**Agent 行为**:
1. k3cloud_describe_service_meta(actionId=2) → ...
2. k3cloud_get_extension_fields(extensionFid=...) → ...
3. k3cloud_add_calculate_rule(...) → success: serviceId=...

**BOS Designer 验证**: [截图 + 描述]

[场景 2-6 类似]

## 已知 limitation

- Calculate AST validator 当前只查函数白名单 + 字段引用 + Python 3 语法 + SQL-style 禁止模式。**未实现**：完整 Python AST 解析（递归表达式 / lambda / .NET 方法链等）。复杂场景可能漏检，BOS 服务端最终会 reject。
- GetInvStock 工具暴露 ~19 typed param。客户实战如发现 v0.2 补充字段，回头扩 SERVICE_META_SCHEMAS。
```

- [ ] **Step 3: Commit demo doc**

```bash
git add docs/demos/
git commit -m "docs(demos): business-rules e2e demo on SAL_SaleOrder dev extension (Plan 5.12.3b)"
```

---

## Self-Review

### Spec coverage（对照 5.12.3 spec doc）

- ✅ Phase 2 bridge ops (4)：list_business_rules / add_entity_service_rule / add_field_update_action / remove_business_rule → Tasks 2.1-2.4
- ✅ Phase 3 agent 工具 (5)：list / delete / describe / add_get_inv_stock_rule / add_calculate_rule → Tasks 3.2-3.5
- ✅ Phase 3 connector wrapper → Task 3.1
- ✅ Phase 4 Calculate validator (route C) + IronPython AST → Task 3.4 (合并到 Phase 3 因为是 add_calculate_rule 的依赖)
- ✅ Phase 5 e2e demo → Task 5.1
- ✅ Phase 4 system prompt 更新 → Task 4.1
- ⚠️ ActionId=3/23/42/70 不实施（用户 v0.1 scope cut，留 v0.2）

### Placeholder scan

- 已扫，无 TBD / TODO / fill-in-later
- "实证日期" 用 `2026-05-XX` 占位（Task 5.1 填实际日期）
- `TEST_EXT_FID` 占位将在 Task 2.1 Step 6 时由用户提供具体 FID（dev 环境实证扩展）

### Type consistency

- 所有 bridge ops 命名 snake_case：`list_business_rules` / `add_entity_service_rule` / `add_field_update_action` / `remove_business_rule`
- 所有 agent 工具命名 `k3cloud_*`：`k3cloud_list_business_rules` / `k3cloud_delete_business_rule` / `k3cloud_describe_service_meta` / `k3cloud_add_get_inv_stock_rule` / `k3cloud_add_calculate_rule`
- ServiceMeta 类名引用一致：`FormBusinessService` (基类，Calculate 用) / `GetInvStockBusinessServiceMeta` (子类)
- mountPoint discriminator 在 spec 设计、connector wrapper、agent 工具三处一致

### 估时对账

| Phase | tasks | 估时 |
|---|---|---|
| Phase 2 (bridge ops) | 4 | 3-4 天 |
| Phase 3 (tools + validators) | 5 | 4-5 天 |
| Phase 4 (prompts) | 1 | 0.5 天 |
| Phase 5 (e2e demo) | 1 | 1-2 天 |
| **合计** | **11** | **8.5-11.5 天** |

符合 5.12.3a memory 给出的 ~9-13 天范围。

### 已知风险

| 风险 | 缓解 |
|---|---|
| connector wrapper 写回路径未实证（Task 3.1 spike 阶段）：现有 add-fields 走的是 typed AST → DCXML 路径,业务规则需要"patched FKERNELXML 整段回推" | Task 3.1 Step 1 spike：连真扩展跑通 → BLOCKED 上报 → 必要时回头补 wire 实证 capture |
| ServiceMeta 反射查类名时，`Kingdee.BOS.Core.Metadata.BusinessService.GetInvStockBusinessServiceMeta` 的命名空间可能在不同 BOS 版本不一致 | bridge 启动时 `LoadServiceMetaTypes` 一次性建索引，按短类名 lookup（不依赖完整命名空间）|
| Calculate validator 太严，挡住合法但少见的 IronPython 模式（lambda / 三目嵌套 / .NET 实例链）| validator 只对**已知禁止模式**报错，未识别的复杂表达式默认放过——BOS 服务端最终把关。错误反馈含 retryHint 让 LLM 自己改 |
| field-existence 校验需要 `connector.getExtensionFields` 返回的 fields 集合包含**所有 entity 的字段**（不仅 head），否则 entry-level 字段引用会误报 | Task 3.3 实施前 grep `getExtensionFields` 确认返回结构包含 entity 分组；如不包含，先加 sub-task 改 connector |
| BOS 跨 entity 字段引用模式（`FBillEntry.F金额` 之类）validator 不支持 | v0.1 跳过实证；如客户场景需要，回头补 |

---

## Execution Handoff

Plan 完稿。下一步执行选择：

1. **Subagent-Driven** — 一 task 一 subagent，每 task 后 review 进 next（11 tasks）
2. **Inline Execution** — batch 跑

考虑到 Plan 5.12.3b 工程量大（8.5-11.5 天）+ memory `feedback_minimize_forks_through_may` 的 fork 用量预算，建议**Inline Execution + checkpoint per phase**，每个 Phase 末段做 quality 自查（不开 subagent）。

完成本 plan 后：
- 5.12.3 整体完成，回到 Plan 5.12 master plan 接 5.12.5 反写规则 / 5.12.6 操作或者直接进 Plan 6 release gate
- 5.12.3 e2e demo 视为 Plan 5.12 第一个完整闭环验证（加字段已有 + 加规则 + 删规则）
