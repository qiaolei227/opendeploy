# Plan 5.11 — 多模型按厂商选择 (Multi-Model Per Provider)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户在每个 LLM 厂商下手动选择具体模型 (DeepSeek 选 V4 Pro 还是 V4 Flash, Claude 选 Opus 4.7 还是 Sonnet 4.6 等),状态栏与 agent 调用使用所选模型的真实参数 (上下文窗口、最大输出),Ollama 因为型号无穷尽采用自由文本输入。

**Architecture:** Provider 数据结构从扁平 (单 `defaultModel` + 单 `contextWindow`) 升级为嵌套 (`models[]` 数组,每条带 `id` / `label` / `contextWindow` / `maxOutput` / `pricing` / `recommended`)。`AppSettings.modelByProvider: Record<providerId, modelId>` 持久化用户的逐厂商选择,缺省时回退到该厂商 `recommended: true` 的那条 (没有就用首条)。Settings 页 LLM section 在选中厂商后展示一个二级下拉框 (Ollama 是 input)。`LlmChatRequest.model` 加字段、preload IPC 透传到 `runAgentLoop` 再到 `client.stream()` (链路已经支持 `model?: string` 参数,只缺末端补)。状态栏按当前选中模型的 `contextWindow` 渲染上下文表,而不是厂商级。

**Tech Stack:** TypeScript / React / Zustand / Electron IPC / 现有 LLM clients (无需改动)

**Scope 红线 (本 plan 不做):**
- 不做模型动态切换 / 自动路由 (已和用户对齐:固定模型,人工选)
- 不做 cost / latency 实测 (`pricing` 字段仅展示用,标"参考价")
- 不做模型卡比较页 / 模型筛选器 — Settings 下拉 + 状态栏文本即够
- 不做模型探活 (假设 API key 配对的厂商,所列模型都可用)

---

## File Structure

**修改:**
- `src/renderer/data/providers.ts` — `LlmProvider.models[]` 数组 + `LlmModel` 类型
- `src/shared/types.ts` — `AppSettings.modelByProvider` + `LlmChatRequest.model` 已有 (无需改)
- `src/renderer/stores/settings-store.ts` — `setModel(providerId, modelId)` action
- `src/main/ipc-llm.ts` — `req.model` 透传到 `runAgentLoop`
- `src/renderer/stores/chat-store.ts` — `sendMessage` 读取 `modelByProvider[providerId]` 注入 IPC payload
- `src/renderer/pages/SettingsPage.tsx` — 选中厂商后展示模型下拉 (Ollama 是 input)
- `src/renderer/components/StatusBar.tsx` — 按选中模型的 `contextWindow` 算上下文条 + 显示模型 short label
- `src/main/llm/types.ts` — `PROVIDER_CONFIGS[*].defaultModel` 仍保留作为后端缺省 (前端没传 model 时 OpenAI/Anthropic client 已有 `req.model ?? opts.defaultModel` 兜底,无需改)
- `src/renderer/i18n/zh-CN.ts` + `en-US.ts` — 新 key (`settings.model`, `settings.modelHint`, `settings.modelOllamaPlaceholder`, `status.modelLabel`)

**新建测试:**
- `tests/renderer/providers-models.test.ts` — providers.ts 形状校验 + helper 函数 (resolveActiveModel)
- `tests/renderer/chat-store-model.test.ts` — `sendMessage` 是否正确把选中 model 透传给 IPC
- `tests/main/ipc-llm-model.test.ts` — IPC handler 是否把 `req.model` 透传给 `runAgentLoop` (用 vi.mock)

**不动:**
- `src/main/llm/openai-client.ts` / `anthropic-client.ts` / `ollama-client.ts` — 已支持 `req.model ?? defaultModel`
- `src/main/agent/loop.ts` — `RunAgentLoopParams.model?: string` 已存在并传给 `client.stream()`
- `src/main/llm/factory.ts` — 不需要改
- 已有 26 个 i18n key 不变

---

## 模型数据 (Task 1 用)

> **注:** 数据基于 2026-04-25 公开渠道整理。Pricing 单位 = ¥/M tokens (input/output),海外厂商保留原币种。`recommended` 标在每家性价比/通用最佳那条 (不是顶配)。`hint` 是 12 字以内 sub-label。`maxOutput` 没找到统一标注的就和 `contextWindow` 同值。

```typescript
// 见 Task 1 完整 providers.ts。摘录:

deepseek:
  - { id: 'deepseek-v4-flash', label: 'DeepSeek V4 Flash', contextWindow: 1_000_000, maxOutput: 16_384, pricing: '¥1.0 / 2.0', hint: '1M · 极速', recommended: true }
  - { id: 'deepseek-v4-pro',   label: 'DeepSeek V4 Pro',   contextWindow: 1_000_000, maxOutput: 16_384, pricing: '¥12 / 24',  hint: '1M · 代码首选' }

claude:
  - { id: 'claude-haiku-4-5-20251001', label: 'Claude Haiku 4.5', contextWindow: 200_000,   maxOutput: 64_000,  pricing: '$1 / 5',    hint: '200K · 速度', recommended: true }
  - { id: 'claude-sonnet-4-6',         label: 'Claude Sonnet 4.6', contextWindow: 1_000_000, maxOutput: 64_000,  pricing: '$3 / 15',  hint: '1M · 主力' }
  - { id: 'claude-opus-4-7',           label: 'Claude Opus 4.7',   contextWindow: 1_000_000, maxOutput: 128_000, pricing: '$5 / 25',  hint: '1M · 顶配' }

glm:
  - { id: 'glm-4.7-flashx', label: 'GLM-4.7 FlashX',  contextWindow: 200_000, maxOutput: 32_000,  pricing: '¥0.5 / 1.5', hint: '200K · 性价比', recommended: true }
  - { id: 'glm-4.7',        label: 'GLM-4.7',         contextWindow: 200_000, maxOutput: 64_000,  pricing: '¥4 / 16',    hint: '200K · 通用' }
  - { id: 'glm-5.1',        label: 'GLM-5.1',         contextWindow: 200_000, maxOutput: 128_000, pricing: '¥8 / 32',    hint: '200K · 长输出' }
  - { id: 'glm-4-long',     label: 'GLM-4 Long',      contextWindow: 1_000_000, maxOutput: 8_192,  pricing: '¥1 / 8',    hint: '1M · 长文本' }

kimi:
  - { id: 'kimi-k2.6', label: 'Kimi K2.6',  contextWindow: 256_000, maxOutput: 16_384, pricing: '¥5.2 / 32.6', hint: '256K · 主力', recommended: true }
  - { id: 'kimi-k2.5', label: 'Kimi K2.5',  contextWindow: 262_144, maxOutput: 16_384, pricing: '¥4.2 / 17.5', hint: '262K · 经济' }

doubao:
  - { id: 'doubao-seed-2.0-pro',  label: 'Doubao Seed 2.0 Pro',  contextWindow: 256_000, maxOutput: 16_384, pricing: '¥0.9 / 9.0', hint: '256K · 主力', recommended: true }
  - { id: 'doubao-seed-2.0-lite', label: 'Doubao Seed 2.0 Lite', contextWindow: 128_000, maxOutput: 16_384, pricing: '¥0.3 / 3.0', hint: '128K · 经济' }
  - { id: 'doubao-seed-1.6',      label: 'Doubao Seed 1.6',      contextWindow: 256_000, maxOutput: 16_384, pricing: '¥0.8 / 8.0', hint: '256K · 上一代' }

minimax:
  - { id: 'MiniMax-M2.7', label: 'MiniMax M2.7', contextWindow: 196_608, maxOutput: 8_192, pricing: '¥2.1 / 8.4', hint: '192K · 主力', recommended: true }

qwen:
  - { id: 'qwen3.6-flash', label: 'Qwen3.6 Flash', contextWindow: 1_000_000, maxOutput: 8_192,  pricing: '¥0.5 / 1.0', hint: '1M · 极速 (待校准)', recommended: true }
  - { id: 'qwen3.6-plus',  label: 'Qwen3.6 Plus',  contextWindow: 1_000_000, maxOutput: 8_192,  pricing: '¥1.8 / 6.0', hint: '1M · 主力 (待校准)' }
  - { id: 'qwen3-max',     label: 'Qwen3 Max',     contextWindow: 256_000,   maxOutput: 8_192,  pricing: '¥20 / 60',   hint: '256K · 顶配 (待校准)' }
  - { id: 'qwen3-coder',   label: 'Qwen3 Coder',   contextWindow: 256_000,   maxOutput: 8_192,  pricing: '¥4 / 16',    hint: '256K · 代码 (待校准)' }

hunyuan:
  - { id: 'hunyuan-turbos-latest',           label: 'Hunyuan TurboS',     contextWindow: 224_000, maxOutput: 8_192, pricing: '¥0.8 / 2.0', hint: '224K · 主力 (待校准)', recommended: true }
  - { id: 'hunyuan-2.0-thinking-20251109',   label: 'Hunyuan 2.0 Thinking', contextWindow: 256_000, maxOutput: 16_384, pricing: '¥3 / 9',  hint: '256K · 推理 (待校准)' }
  - { id: 'hunyuan-large',                    label: 'Hunyuan Large',      contextWindow: 200_000, maxOutput: 8_192, pricing: '¥4 / 12',   hint: '200K · 通用 (待校准)' }
  - { id: 'hunyuan-lite',                     label: 'Hunyuan Lite',       contextWindow: 128_000, maxOutput: 8_192, pricing: 'free',      hint: '128K · 免费 (待校准)' }

gpt:
  - { id: 'gpt-5.5',      label: 'GPT-5.5',      contextWindow: 1_050_000, maxOutput: 100_000, pricing: '$5 / 25',  hint: '1M · 主力 (待校准)', recommended: true }
  - { id: 'gpt-5.5-pro',  label: 'GPT-5.5 Pro',  contextWindow: 1_050_000, maxOutput: 100_000, pricing: '$15 / 75', hint: '1M · 顶配 (待校准)' }
  - { id: 'gpt-5.4-mini', label: 'GPT-5.4 Mini', contextWindow: 200_000,   maxOutput: 16_384,  pricing: '$0.3 / 1.2', hint: '200K · 经济 (待校准)' }

ollama:
  models: []  // 空数组 — UI 看到 ollama 走 input 而不是 select
  modelInputDefault: 'qwen2.5-coder'
```

> **校准约定:** "(待校准)" 后缀放在 hint 末尾,提醒用户该值未在 2026-04 通过厂商官方渠道二次确认。Plan 6 发布前可以删,也可以留作社区贡献入口。

---

## 兼容性策略

- **旧设置升级路径:** 既有 `AppSettings` 没有 `modelByProvider`,首次升级后通过 `resolveActiveModel(providerId, modelByProvider)` helper 自动 fall back 到 provider.recommended (或 models[0])。无需 migration 脚本,缺省值在读时计算。
- **后端容错:** 如果用户 settings 里存的 `modelByProvider['deepseek'] = 'foo-bar'` 但 providers.ts 里删了该模型,resolveActiveModel 检测到非法 id 时 fall back 到 recommended,不报错 (用户重启 / 升级期间常见)。
- **PROVIDER_CONFIGS.defaultModel:** 保留 — 当 IPC payload 没传 model (e.g. 来自旧 renderer 或单元测试) 时,client 端 `req.model ?? opts.defaultModel` 仍然兜底。后端不依赖 modelByProvider 存在。

---

### Task 1: providers.ts 数据结构升级 + 模型列表填充

**Files:**
- Modify: `src/renderer/data/providers.ts` (重写)
- Test: `tests/renderer/providers-models.test.ts` (NEW)

- [ ] **Step 1: 写失败测试 — 形状 + helper**

```typescript
// tests/renderer/providers-models.test.ts
import { describe, it, expect } from 'vitest';
import { PROVIDERS, PROVIDER_BY_ID, resolveActiveModel } from '../../src/renderer/data/providers';

describe('providers + models', () => {
  it('every provider declares models[] (Ollama may be empty)', () => {
    for (const p of PROVIDERS) {
      expect(p.models).toBeDefined();
      expect(Array.isArray(p.models)).toBe(true);
      if (p.id !== 'ollama') {
        expect(p.models.length).toBeGreaterThan(0);
        // recommended 标记在每家恰好一条
        expect(p.models.filter((m) => m.recommended).length).toBe(1);
      }
    }
  });

  it('every model has id / label / contextWindow / maxOutput / pricing / hint', () => {
    for (const p of PROVIDERS) {
      for (const m of p.models) {
        expect(m.id).toBeTruthy();
        expect(m.label).toBeTruthy();
        expect(m.contextWindow).toBeGreaterThan(0);
        expect(m.maxOutput).toBeGreaterThan(0);
        expect(m.pricing).toBeTruthy();
        expect(m.hint).toBeTruthy();
      }
    }
  });

  it('Ollama declares modelInputDefault', () => {
    const ollama = PROVIDER_BY_ID['ollama'];
    expect(ollama.models).toEqual([]);
    expect(ollama.modelInputDefault).toBeTruthy();
  });

  it('resolveActiveModel returns user choice when valid', () => {
    const m = resolveActiveModel('deepseek', { deepseek: 'deepseek-v4-pro' });
    expect(m?.id).toBe('deepseek-v4-pro');
  });

  it('resolveActiveModel falls back to recommended when stored id missing', () => {
    const m = resolveActiveModel('deepseek', { deepseek: 'no-such-model' });
    expect(m?.id).toBe('deepseek-v4-flash'); // recommended in our table
  });

  it('resolveActiveModel falls back to recommended when not stored at all', () => {
    const m = resolveActiveModel('claude', {});
    expect(m?.id).toBe('claude-haiku-4-5-20251001'); // recommended
  });

  it('resolveActiveModel for ollama returns null (用 modelInputDefault 走另一路径)', () => {
    const m = resolveActiveModel('ollama', {});
    expect(m).toBeNull();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test tests/renderer/providers-models.test.ts`
Expected: FAIL — `models is undefined`, `resolveActiveModel is not a function`

- [ ] **Step 3: 重写 providers.ts**

```typescript
// src/renderer/data/providers.ts
export type ProviderRegion = 'CN' | 'Overseas' | 'Local';

export interface LlmModel {
  /** Provider-side model id, sent to API as `model` param. */
  id: string;
  /** Display name. */
  label: string;
  /** Max input context in tokens. */
  contextWindow: number;
  /** Max output tokens (单轮 completion 上限). */
  maxOutput: number;
  /** "input/output" 单位 ¥ 或 $ per M tokens, 仅展示. */
  pricing: string;
  /** ≤12 字 sub-label, e.g. "1M · 极速". */
  hint: string;
  /** Mark exactly one model per provider as recommended (sets default). */
  recommended?: boolean;
}

export interface LlmProvider {
  id: string;
  dot: string;
  letter: string;
  label: string;
  /** Provider-level short name (e.g. 'DeepSeek'). 模型 short 由 LlmModel.label 承担. */
  short: string;
  /** Provider-level descriptive sub (e.g. '国内直连 · 代码首选') — 不再含模型名. */
  sub: string;
  region: ProviderRegion;
  recommended?: boolean;
  /** Available models. Empty for Ollama (free-form input). */
  models: LlmModel[];
  /** Ollama only — 默认填到 input 框的型号. */
  modelInputDefault?: string;
}

export const PROVIDERS: LlmProvider[] = [
  {
    id: 'deepseek', dot: 'deepseek', letter: 'D',
    label: 'DeepSeek', short: 'DeepSeek', sub: '国内直连 · 代码首选',
    region: 'CN', recommended: true,
    models: [
      { id: 'deepseek-v4-flash', label: 'DeepSeek V4 Flash', contextWindow: 1_000_000, maxOutput: 16_384, pricing: '¥1.0 / 2.0', hint: '1M · 极速', recommended: true },
      { id: 'deepseek-v4-pro',   label: 'DeepSeek V4 Pro',   contextWindow: 1_000_000, maxOutput: 16_384, pricing: '¥12 / 24',  hint: '1M · 代码首选' }
    ]
  },
  {
    id: 'qwen', dot: 'qwen', letter: 'Q',
    label: '通义 Qwen', short: '通义 Qwen', sub: '阿里 DashScope · 1M 上下文',
    region: 'CN',
    models: [
      { id: 'qwen3.6-flash', label: 'Qwen3.6 Flash', contextWindow: 1_000_000, maxOutput: 8_192, pricing: '¥0.5 / 1.0', hint: '1M · 极速 (待校准)', recommended: true },
      { id: 'qwen3.6-plus',  label: 'Qwen3.6 Plus',  contextWindow: 1_000_000, maxOutput: 8_192, pricing: '¥1.8 / 6.0', hint: '1M · 主力 (待校准)' },
      { id: 'qwen3-max',     label: 'Qwen3 Max',     contextWindow: 256_000,   maxOutput: 8_192, pricing: '¥20 / 60',   hint: '256K · 顶配 (待校准)' },
      { id: 'qwen3-coder',   label: 'Qwen3 Coder',   contextWindow: 256_000,   maxOutput: 8_192, pricing: '¥4 / 16',    hint: '256K · 代码 (待校准)' }
    ]
  },
  {
    id: 'glm', dot: 'glm', letter: '智',
    label: '智谱 GLM', short: '智谱 GLM', sub: '智谱 BigModel · 性价比高',
    region: 'CN',
    models: [
      { id: 'glm-4.7-flashx', label: 'GLM-4.7 FlashX', contextWindow: 200_000,   maxOutput: 32_000,  pricing: '¥0.5 / 1.5', hint: '200K · 性价比', recommended: true },
      { id: 'glm-4.7',        label: 'GLM-4.7',        contextWindow: 200_000,   maxOutput: 64_000,  pricing: '¥4 / 16',    hint: '200K · 通用' },
      { id: 'glm-5.1',        label: 'GLM-5.1',        contextWindow: 200_000,   maxOutput: 128_000, pricing: '¥8 / 32',    hint: '200K · 长输出' },
      { id: 'glm-4-long',     label: 'GLM-4 Long',     contextWindow: 1_000_000, maxOutput: 8_192,   pricing: '¥1 / 8',     hint: '1M · 长文本' }
    ]
  },
  {
    id: 'kimi', dot: 'kimi', letter: 'K',
    label: 'Moonshot Kimi', short: 'Kimi', sub: 'Moonshot · 长上下文 + 代码',
    region: 'CN',
    models: [
      { id: 'kimi-k2.6', label: 'Kimi K2.6', contextWindow: 256_000, maxOutput: 16_384, pricing: '¥5.2 / 32.6', hint: '256K · 主力', recommended: true },
      { id: 'kimi-k2.5', label: 'Kimi K2.5', contextWindow: 262_144, maxOutput: 16_384, pricing: '¥4.2 / 17.5', hint: '262K · 经济' }
    ]
  },
  {
    id: 'doubao', dot: 'doubao', letter: '豆',
    label: '字节 豆包', short: '豆包', sub: '火山方舟 · 国内直连',
    region: 'CN',
    models: [
      { id: 'doubao-seed-2-0-pro-260215',  label: 'Doubao Seed 2.0 Pro',  contextWindow: 256_000, maxOutput: 16_384, pricing: '¥0.9 / 9.0', hint: '256K · 主力', recommended: true },
      { id: 'doubao-seed-2-0-lite-260215', label: 'Doubao Seed 2.0 Lite', contextWindow: 128_000, maxOutput: 16_384, pricing: '¥0.3 / 3.0', hint: '128K · 经济' },
      { id: 'doubao-seed-1-6-260215',      label: 'Doubao Seed 1.6',      contextWindow: 256_000, maxOutput: 16_384, pricing: '¥0.8 / 8.0', hint: '256K · 上一代' }
    ]
  },
  {
    id: 'hunyuan', dot: 'hunyuan', letter: '腾',
    label: '腾讯 混元', short: '混元', sub: '腾讯云 · 多模态',
    region: 'CN',
    models: [
      { id: 'hunyuan-turbos-latest',         label: 'Hunyuan TurboS',       contextWindow: 224_000, maxOutput: 8_192,  pricing: '¥0.8 / 2.0', hint: '224K · 主力 (待校准)', recommended: true },
      { id: 'hunyuan-2.0-thinking-20251109', label: 'Hunyuan 2.0 Thinking', contextWindow: 256_000, maxOutput: 16_384, pricing: '¥3 / 9',     hint: '256K · 推理 (待校准)' },
      { id: 'hunyuan-large',                  label: 'Hunyuan Large',        contextWindow: 200_000, maxOutput: 8_192,  pricing: '¥4 / 12',    hint: '200K · 通用 (待校准)' },
      { id: 'hunyuan-lite',                   label: 'Hunyuan Lite',         contextWindow: 128_000, maxOutput: 8_192,  pricing: 'free',       hint: '128K · 免费 (待校准)' }
    ]
  },
  {
    id: 'minimax', dot: 'minimax', letter: 'M',
    label: 'MiniMax', short: 'MiniMax', sub: '海螺 AI · 长输出',
    region: 'CN',
    models: [
      { id: 'MiniMax-M2.7', label: 'MiniMax M2.7', contextWindow: 196_608, maxOutput: 8_192, pricing: '¥2.1 / 8.4', hint: '192K · 主力', recommended: true }
    ]
  },
  {
    id: 'claude', dot: 'claude', letter: 'A',
    label: 'Anthropic Claude', short: 'Claude', sub: '海外需代理 · 推理强',
    region: 'Overseas',
    models: [
      { id: 'claude-haiku-4-5-20251001', label: 'Claude Haiku 4.5',  contextWindow: 200_000,   maxOutput: 64_000,  pricing: '$1 / 5',  hint: '200K · 速度', recommended: true },
      { id: 'claude-sonnet-4-6',         label: 'Claude Sonnet 4.6', contextWindow: 1_000_000, maxOutput: 64_000,  pricing: '$3 / 15', hint: '1M · 主力' },
      { id: 'claude-opus-4-7',           label: 'Claude Opus 4.7',   contextWindow: 1_000_000, maxOutput: 128_000, pricing: '$5 / 25', hint: '1M · 顶配' }
    ]
  },
  {
    id: 'gpt', dot: 'openai', letter: 'G',
    label: 'OpenAI GPT', short: 'GPT', sub: '海外需代理 · 通用强',
    region: 'Overseas',
    models: [
      { id: 'gpt-5.5',      label: 'GPT-5.5',      contextWindow: 1_050_000, maxOutput: 100_000, pricing: '$5 / 25',    hint: '1M · 主力 (待校准)', recommended: true },
      { id: 'gpt-5.5-pro',  label: 'GPT-5.5 Pro',  contextWindow: 1_050_000, maxOutput: 100_000, pricing: '$15 / 75',   hint: '1M · 顶配 (待校准)' },
      { id: 'gpt-5.4-mini', label: 'GPT-5.4 Mini', contextWindow: 200_000,   maxOutput: 16_384,  pricing: '$0.3 / 1.2', hint: '200K · 经济 (待校准)' }
    ]
  },
  {
    id: 'ollama', dot: 'ollama', letter: 'O',
    label: 'Ollama 本地', short: 'Ollama', sub: '完全离线 · 自定义模型',
    region: 'Local',
    models: [],
    modelInputDefault: 'qwen2.5-coder'
  }
];

export const PROVIDER_BY_ID: Record<string, LlmProvider> = Object.fromEntries(
  PROVIDERS.map((p) => [p.id, p])
);

/**
 * Pick the active model for a provider given the user's stored selection.
 * Falls back to recommended → first-in-list → null.
 * Returns null for Ollama (caller uses provider.modelInputDefault + free-form input).
 */
export function resolveActiveModel(
  providerId: string,
  modelByProvider: Record<string, string> | undefined
): LlmModel | null {
  const provider = PROVIDER_BY_ID[providerId];
  if (!provider || provider.models.length === 0) return null;
  const stored = modelByProvider?.[providerId];
  if (stored) {
    const found = provider.models.find((m) => m.id === stored);
    if (found) return found;
  }
  return provider.models.find((m) => m.recommended) ?? provider.models[0];
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm test tests/renderer/providers-models.test.ts`
Expected: PASS (7/7)

- [ ] **Step 5: 跑 typecheck 找出 callers 编译错**

Run: `pnpm typecheck`
Expected: 多处报错 — `Property 'defaultModel' does not exist`, `Property 'lat' does not exist` 等。Task 5/6/7 会修。先记下报错位置。

- [ ] **Step 6: Commit**

```bash
git add src/renderer/data/providers.ts tests/renderer/providers-models.test.ts
git commit -m "feat(providers): introduce models[] per provider with recommended + resolveActiveModel"
```

---

### Task 2: AppSettings.modelByProvider + settings store

**Files:**
- Modify: `src/shared/types.ts`
- Modify: `src/renderer/stores/settings-store.ts`
- Test: 现有 `tests/renderer/settings-store.test.ts` 加用例 (如果不存在就建)

- [ ] **Step 1: 写失败测试**

```typescript
// 加到 tests/renderer/settings-store.test.ts (或新建)
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { useSettingsStore } from '../../src/renderer/stores/settings-store';

describe('settings store — model selection', () => {
  beforeEach(() => {
    // 重置 store + mock window.opendeploy.saveSettings
    (window as any).opendeploy = {
      saveSettings: vi.fn().mockResolvedValue(undefined),
      getSettings: vi.fn().mockResolvedValue({ language: 'zh-CN', theme: 'system' })
    };
    useSettingsStore.setState({ settings: { language: 'zh-CN', theme: 'system' } });
  });

  it('setModel 持久化 modelByProvider[providerId]', async () => {
    await useSettingsStore.getState().setModel('deepseek', 'deepseek-v4-pro');
    const s = useSettingsStore.getState().settings;
    expect(s.modelByProvider).toEqual({ deepseek: 'deepseek-v4-pro' });
    expect((window as any).opendeploy.saveSettings).toHaveBeenCalledWith(
      expect.objectContaining({ modelByProvider: { deepseek: 'deepseek-v4-pro' } })
    );
  });

  it('setModel 多次调用合并不同 provider', async () => {
    await useSettingsStore.getState().setModel('deepseek', 'deepseek-v4-pro');
    await useSettingsStore.getState().setModel('claude', 'claude-opus-4-7');
    const s = useSettingsStore.getState().settings;
    expect(s.modelByProvider).toEqual({
      deepseek: 'deepseek-v4-pro',
      claude: 'claude-opus-4-7'
    });
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test tests/renderer/settings-store.test.ts`
Expected: FAIL — `setModel is not a function`

- [ ] **Step 3: 改 AppSettings 类型**

在 `src/shared/types.ts` 的 `AppSettings` 接口加字段:

```typescript
export interface AppSettings {
  language: Language;
  theme: Theme;
  llmProvider?: string;
  apiKeys?: Record<string, string>;
  /**
   * 用户在每个 LLM 厂商下选择的具体模型 id (e.g. {deepseek: 'deepseek-v4-pro'}).
   * 缺省时通过 resolveActiveModel 回退到该 provider 的 recommended 模型.
   * Ollama 走 ollamaModelInput 而不是这里 (因为 Ollama 是自由文本).
   */
  modelByProvider?: Record<string, string>;
  /** Ollama 自定义模型名 (用户在 Settings 输入框填的). 缺省走 PROVIDERS.find(ollama).modelInputDefault. */
  ollamaModelInput?: string;
  knowledgeSources?: KnowledgeSource[];
  projects?: Project[];
  activeProjectId?: string;
}
```

- [ ] **Step 4: settings-store 加 setModel + setOllamaModelInput action**

Read 当前 store 找 setApiKey 的实现做参考:

Run: `pnpm exec grep -n "setApiKey" src/renderer/stores/settings-store.ts`

仿 `setApiKey` 的 pattern 加 `setModel(providerId, modelId)` 和 `setOllamaModelInput(value)`:

```typescript
setModel: async (providerId: string, modelId: string) => {
  const current = get().settings;
  const next: AppSettings = {
    ...current,
    modelByProvider: { ...(current.modelByProvider ?? {}), [providerId]: modelId }
  };
  await window.opendeploy.saveSettings(next);
  set({ settings: next });
},
setOllamaModelInput: async (value: string) => {
  const current = get().settings;
  const next: AppSettings = { ...current, ollamaModelInput: value };
  await window.opendeploy.saveSettings(next);
  set({ settings: next });
}
```

接口 `SettingsStore` 同步加上方法签名。

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm test tests/renderer/settings-store.test.ts`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/shared/types.ts src/renderer/stores/settings-store.ts tests/renderer/settings-store.test.ts
git commit -m "feat(settings): add modelByProvider + ollamaModelInput with setModel action"
```

---

### Task 3: IPC + chat-store 透传选中 model 给后端

**Files:**
- Modify: `src/renderer/stores/chat-store.ts`
- Test: `tests/renderer/chat-store-model.test.ts` (NEW)

> `LlmChatRequest` 已有 `apiKey` 字段;需要新增 `model?: string`。先看现有 IPC handler 接收什么。

- [ ] **Step 1: 写失败测试**

```typescript
// tests/renderer/chat-store-model.test.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { useChatStore } from '../../src/renderer/stores/chat-store';
import { useSettingsStore } from '../../src/renderer/stores/settings-store';

describe('chat-store sendMessage — model透传', () => {
  let llmSendMessage: ReturnType<typeof vi.fn>;

  beforeEach(() => {
    llmSendMessage = vi.fn().mockResolvedValue({ requestId: 'req_1' });
    (window as any).opendeploy = {
      llmSendMessage,
      llmOnStream: () => () => {} // unsubscribe noop
    };
    useChatStore.setState({ messages: [], isStreaming: false, error: null, currentRequestId: null, conversationId: null });
    useSettingsStore.setState({
      settings: {
        language: 'zh-CN',
        theme: 'system',
        modelByProvider: { deepseek: 'deepseek-v4-pro' }
      }
    });
  });

  it('把 modelByProvider[deepseek] 透传给 IPC payload.model', async () => {
    await useChatStore.getState().sendMessage('hi', 'deepseek', 'sk-test');
    expect(llmSendMessage).toHaveBeenCalledWith(expect.objectContaining({
      providerId: 'deepseek',
      model: 'deepseek-v4-pro'
    }));
  });

  it('未配置时回退到 recommended 模型 id', async () => {
    useSettingsStore.setState({
      settings: { language: 'zh-CN', theme: 'system' } // 没 modelByProvider
    });
    await useChatStore.getState().sendMessage('hi', 'claude', 'sk-test');
    expect(llmSendMessage).toHaveBeenCalledWith(expect.objectContaining({
      model: 'claude-haiku-4-5-20251001' // recommended for claude
    }));
  });

  it('Ollama 透传 ollamaModelInput', async () => {
    useSettingsStore.setState({
      settings: { language: 'zh-CN', theme: 'system', ollamaModelInput: 'qwen2.5:14b' }
    });
    await useChatStore.getState().sendMessage('hi', 'ollama', undefined);
    expect(llmSendMessage).toHaveBeenCalledWith(expect.objectContaining({
      providerId: 'ollama',
      model: 'qwen2.5:14b'
    }));
  });

  it('Ollama 未配置时回退到 modelInputDefault', async () => {
    useSettingsStore.setState({
      settings: { language: 'zh-CN', theme: 'system' }
    });
    await useChatStore.getState().sendMessage('hi', 'ollama', undefined);
    expect(llmSendMessage).toHaveBeenCalledWith(expect.objectContaining({
      model: 'qwen2.5-coder' // modelInputDefault
    }));
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test tests/renderer/chat-store-model.test.ts`
Expected: FAIL — `expected ... to contain "model"`

- [ ] **Step 3: 加 LlmChatRequest.model 字段 + IPC handler 透传**

`src/shared/types.ts`:

```typescript
export interface LlmChatRequest {
  conversationId?: string;
  providerId: string;
  apiKey?: string;
  /** Model id override. 缺省时 client 端用 PROVIDER_CONFIGS[providerId].defaultModel. */
  model?: string;
  userMessage: string;
}
```

`src/main/ipc-llm.ts` 第 127 行附近 `runAgentLoop({...})` 调用,加上:

```typescript
const finalMessages = await runAgentLoop({
  client,
  tools: registry,
  initialMessages: history,
  providerId: req.providerId,
  apiKey: req.apiKey,
  model: req.model,  // ← 新增
  systemPrompt,
  ...
});
```

- [ ] **Step 4: chat-store sendMessage 调用 IPC 时注入 model**

```typescript
// src/renderer/stores/chat-store.ts 在 sendMessage 内
import { resolveActiveModel, PROVIDER_BY_ID } from '@renderer/data/providers';
import { useSettingsStore } from './settings-store';

// ... 在 sendMessage 实现里, 调 llmSendMessage 之前:
const settings = useSettingsStore.getState().settings;
let modelId: string | undefined;
if (providerId === 'ollama') {
  const provider = PROVIDER_BY_ID['ollama'];
  modelId = settings.ollamaModelInput?.trim() || provider?.modelInputDefault;
} else {
  modelId = resolveActiveModel(providerId, settings.modelByProvider)?.id;
}

// 然后:
const { requestId } = await window.opendeploy.llmSendMessage({
  conversationId: get().conversationId ?? undefined,
  providerId,
  apiKey,
  model: modelId,
  userMessage: text
});
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm test tests/renderer/chat-store-model.test.ts`
Expected: PASS (4/4)

- [ ] **Step 6: 跑现有 IPC handler 测试确认没回归**

Run: `pnpm test tests/main/ -- --reporter=basic`
Expected: 现有 ipc-llm 相关测试不应炸 (model 是 optional, 不传也兼容)

- [ ] **Step 7: Commit**

```bash
git add src/shared/types.ts src/main/ipc-llm.ts src/renderer/stores/chat-store.ts tests/renderer/chat-store-model.test.ts
git commit -m "feat(chat): pass selected model id through IPC to agent loop"
```

---

### Task 4: SettingsPage — 模型下拉框 (Ollama 是 input)

**Files:**
- Modify: `src/renderer/pages/SettingsPage.tsx`
- Modify: `src/renderer/i18n/zh-CN.ts` + `en-US.ts`

- [ ] **Step 1: 加 i18n keys (zh + en parity)**

`src/renderer/i18n/zh-CN.ts` 在 `settings` 节点下加:

```typescript
model: '模型',
modelHint: '同一厂商下不同模型的上下文窗口、价格、能力可能差很多',
modelOllamaPlaceholder: '例: qwen2.5-coder, llama3.1, deepseek-r1',
modelOllamaHint: 'Ollama 模型名是你本机 `ollama list` 里的名字',
contextLabel: '上下文',
maxOutputLabel: '最大输出',
priceLabel: '参考价',
recommendedShort: '推荐'
```

en-US 对应:

```typescript
model: 'Model',
modelHint: 'Different models from the same provider may differ widely in context, price, and capability',
modelOllamaPlaceholder: 'e.g. qwen2.5-coder, llama3.1, deepseek-r1',
modelOllamaHint: 'Ollama model name = whatever shows in your local `ollama list`',
contextLabel: 'Context',
maxOutputLabel: 'Max output',
priceLabel: 'Indicative price',
recommendedShort: 'Recommended'
```

- [ ] **Step 2: 改 SettingsPage 的 LlmSection — 移除 lat/cost 行,加模型下拉**

在 `LlmSection()` 里:

a) Provider 卡片不再显示 lat/cost (这两个字段 providers.ts 已删),改显示该 provider 的模型数量:

```tsx
<div className="prov-row">
  <span className="muted">{p.models.length > 0 ? `${p.models.length} models` : 'Custom'}</span>
  <span className={`chip${...}`}>{t(`settings.regions.${p.region}`)}</span>
</div>
```

b) "API Key" section 之前 (或之后) 加 "Model" section:

```tsx
{!isOllama && (
  <section style={{ marginBottom: 24 }}>
    <h3 style={{ margin: '0 0 4px', fontSize: 16 }}>{t('settings.model')}</h3>
    <p className="muted" style={{ marginTop: 0, fontSize: 13 }}>{t('settings.modelHint')}</p>
    <div className="setting-row" style={{ borderBottom: 'none', alignItems: 'flex-start' }}>
      <div>
        <div className="lbl">{provider.label}</div>
      </div>
      <div className="ctl" style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
        <select
          value={selectedModelId}
          onChange={(e) => { void handleModelChange(e.target.value); }}
          style={{ minWidth: 280, padding: '6px 8px' }}
        >
          {provider.models.map((m) => (
            <option key={m.id} value={m.id}>
              {m.label}{m.recommended ? ` (${t('settings.recommendedShort')})` : ''} — {m.hint}
            </option>
          ))}
        </select>
        {selectedModel && (
          <div className="muted" style={{ fontSize: 12, lineHeight: 1.6 }}>
            {t('settings.contextLabel')}: {(selectedModel.contextWindow / 1000).toFixed(0)}K · 
            {t('settings.maxOutputLabel')}: {(selectedModel.maxOutput / 1000).toFixed(1)}K · 
            {t('settings.priceLabel')}: {selectedModel.pricing}
          </div>
        )}
      </div>
    </div>
  </section>
)}

{isOllama && (
  <section style={{ marginBottom: 24 }}>
    <h3 style={{ margin: '0 0 4px', fontSize: 16 }}>{t('settings.model')}</h3>
    <p className="muted" style={{ marginTop: 0, fontSize: 13 }}>{t('settings.modelOllamaHint')}</p>
    <div className="setting-row" style={{ borderBottom: 'none' }}>
      <div><div className="lbl">{t('settings.model')}</div></div>
      <div className="ctl">
        <input
          type="text"
          value={ollamaInput}
          onChange={(e) => setOllamaInput(e.target.value)}
          onBlur={() => { void handleOllamaInputBlur(); }}
          placeholder={t('settings.modelOllamaPlaceholder')}
          style={{ minWidth: 280 }}
        />
      </div>
    </div>
  </section>
)}
```

c) 加状态 + handlers:

```tsx
const setModel = useSettingsStore((s) => s.setModel);
const setOllamaModelInput = useSettingsStore((s) => s.setOllamaModelInput);
const provider = PROVIDER_BY_ID[selectedProviderId];

const activeModel = resolveActiveModel(selectedProviderId, settings.modelByProvider);
const [selectedModelId, setSelectedModelId] = useState<string>(activeModel?.id ?? '');
const selectedModel = provider?.models.find((m) => m.id === selectedModelId);

const [ollamaInput, setOllamaInput] = useState<string>(
  settings.ollamaModelInput ?? PROVIDER_BY_ID['ollama']?.modelInputDefault ?? ''
);

// 切 provider 时同步 selectedModelId 重新解析
useEffect(() => {
  const m = resolveActiveModel(selectedProviderId, settings.modelByProvider);
  setSelectedModelId(m?.id ?? '');
}, [selectedProviderId, settings.modelByProvider]);

const handleModelChange = async (id: string): Promise<void> => {
  setSelectedModelId(id);
  await setModel(selectedProviderId, id);
};
const handleOllamaInputBlur = async (): Promise<void> => {
  if (ollamaInput.trim()) await setOllamaModelInput(ollamaInput.trim());
};
```

- [ ] **Step 3: 跑 typecheck 确认 SettingsPage 编译过**

Run: `pnpm typecheck`
Expected: SettingsPage 不再报 `defaultModel` / `lat` / `cost` 错误。其他文件仍可能报错 (Task 5/6 处理)。

- [ ] **Step 4: 跑现有 settings 测试**

Run: `pnpm test tests/renderer/`
Expected: 既有 settings 相关测试不回归。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/pages/SettingsPage.tsx src/renderer/i18n/
git commit -m "feat(settings-ui): add per-provider model dropdown + Ollama free-form input"
```

---

### Task 5: StatusBar — 用选中模型的 contextWindow + 显示模型 short

**Files:**
- Modify: `src/renderer/components/StatusBar.tsx`

- [ ] **Step 1: 改 providerLabel + maxTokens 计算**

```tsx
import { PROVIDER_BY_ID, resolveActiveModel } from '@renderer/data/providers';
import { useSettingsStore } from '@renderer/stores/settings-store';

// 在组件内:
const settings = useSettingsStore((s) => s.settings);
const provider = llmProviderId ? PROVIDER_BY_ID[llmProviderId] : undefined;
let activeModel = llmProviderId ? resolveActiveModel(llmProviderId, settings.modelByProvider) : null;

let providerLabel: string;
let maxTokens: number;
if (!provider) {
  providerLabel = t('status.llmNotConfigured');
  maxTokens = 0;
} else if (provider.id === 'ollama') {
  const customName = settings.ollamaModelInput?.trim() || provider.modelInputDefault || 'ollama';
  providerLabel = `${provider.short} · ${customName}`;
  // Ollama 上下文未知,用一个保守缺省;由 ollamaContextDefault 提供
  maxTokens = 32_000;
} else if (activeModel) {
  providerLabel = `${provider.short} · ${activeModel.label}`;
  maxTokens = activeModel.contextWindow;
} else {
  providerLabel = provider.short;
  maxTokens = 0;
}
```

- [ ] **Step 2: 跑 typecheck**

Run: `pnpm typecheck`
Expected: StatusBar 不再报 `Property 'contextWindow' does not exist on type 'LlmProvider'` 错误。

- [ ] **Step 3: 验证 status bar 视觉显示** (手动 dev 模式)

Run: `pnpm dev`
- 切到 DeepSeek + 选 V4 Flash → 状态栏应显示 "DeepSeek · DeepSeek V4 Flash" + "0 / 1M"
- 切到 Claude + 选 Opus 4.7 → "Claude · Claude Opus 4.7" + "0 / 1M"
- 切到 Ollama + 改成 llama3.1 → "Ollama · llama3.1" + "0 / 32k"

- [ ] **Step 4: Commit**

```bash
git add src/renderer/components/StatusBar.tsx
git commit -m "feat(statusbar): show selected model name + use model.contextWindow"
```

---

### Task 6: Wizard 适配 + 编译错清零

**Files:**
- Modify: `src/renderer/pages/WizardPage.tsx` (如果引用了被删字段)
- Modify: 任何其他 typecheck 报错的文件

- [ ] **Step 1: 跑 full typecheck**

Run: `pnpm typecheck`
Expected: 看完所有报错 — 主要在 WizardPage / 任何引用 `provider.lat` / `provider.cost` / `provider.contextWindow` / `provider.sub` (现在 sub 仍存在但措辞改了) 的地方。

- [ ] **Step 2: WizardPage 修复**

如果 Wizard 显示 provider lat/cost 卡片,改成显示 "models.length 个模型" 或者 "推荐: <recommended-model.label>"。Wizard 不强制选模型 — 走完 Wizard 默认 recommended,用户可去 Settings 改。

- [ ] **Step 3: 跑全部 test 找回归**

Run: `pnpm test`
Expected: 全部 PASS。如果有 mock 用了旧 LlmProvider 形状,把 mock 加上 `models: []`。

- [ ] **Step 4: 跑 typecheck 确认 0 错**

Run: `pnpm typecheck`
Expected: 0 errors

- [ ] **Step 5: Commit**

```bash
git add -u
git commit -m "fix: adapt wizard + remaining callers to LlmProvider.models[] shape"
```

---

### Task 7: 端到端手动验证

**Files:** 无代码改动 — 验证清单

- [x] **Step 1: 跑 dev 模式**

Run: `pnpm dev`

- [x] **Step 2: 验收清单**

- [x] Settings → LLM Provider → 选 DeepSeek → 模型下拉显示 V4 Flash + V4 Pro,默认 V4 Flash (recommended)
- [x] 切到 V4 Pro → Settings 关再开,选中仍是 V4 Pro (持久化)
- [x] 状态栏显示 "DeepSeek · DeepSeek V4 Pro" 和 "x / 1M"
- [x] 切到 Claude → 模型下拉显示 Haiku 4.5 / Sonnet 4.6 / Opus 4.7,默认 Haiku 4.5
- [x] 选 Opus 4.7,发一条消息 → 看 `~/.opendeploy/logs/app.log` 不应有 "model not found" 之类错误 (假设已配 API key)
- [x] 切到 Ollama → 显示 input 框,默认填 `qwen2.5-coder`
- [x] 改成 `llama3.1` → 失焦后保存 → 状态栏显示 "Ollama · llama3.1"
- [x] 上下文表 (status bar) — Claude 选 Opus 时显示 1M, 选 Haiku 时显示 200K (切换后立即更新)
- [x] Settings 再切语言到 en-US → 模型下拉的 hint / 推荐标记英文显示

- [x] **Step 3: 如有发现 bug → 修 → 再跑一遍**

- [x] **Step 4: 没有 bug → 标记 Plan 5.11 完成**

---

## Self-Review

**Spec coverage:**
- ✅ 用户需求"用户自己选用哪个模型" → Task 4 模型下拉
- ✅ 用户需求"参数请搞正确,比如加个和上下文大小" → Task 1 每模型 contextWindow + maxOutput + Task 5 状态栏取自模型
- ✅ Ollama 特殊处理 → Task 1 + Task 4 input 路径
- ✅ recommended 标记 → Task 1 数据 + Task 4 UI 显示

**Placeholder scan:** 已检 — 所有 step 都有具体代码 / 具体命令 / 具体期望。

**Type consistency:** `LlmModel.id` / `resolveActiveModel(providerId, modelByProvider) → LlmModel | null` / `AppSettings.modelByProvider: Record<string, string>` 这一组在 Task 1 / 2 / 3 / 4 / 5 都用一致命名。

**已知 follow-up (不阻塞本 plan):**
- Plan 6 之前需要把 "(待校准)" 那几个 provider 的 pricing 通过厂商官方文档实证一遍
- Wizard 二期可加 "选完厂商后选模型" 步骤 — 当前是默认 recommended, 用户去 Settings 改
- Status bar 上下文条对 Ollama 是硬编码 32K, 未来可让用户在 Ollama 设置里覆盖 contextWindow

---

## Execution Handoff

Plan 已保存到 `docs/plans/2026-04-25-plan-5.11-multi-model-per-provider.md`。

两种执行选项:
1. **Subagent-Driven (推荐)** — 每个 Task 派一个 fresh subagent + 两阶段 review (spec → quality)
2. **Inline Execution** — 当前会话顺序跑

请告诉我选哪个,以及是否要在开跑前调整任何 Task 范围 / 模型数据。
